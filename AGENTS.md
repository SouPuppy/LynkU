# LynkU 项目协作规则

- 当前唯一维护目录为 LynkU，由 Lucky 迁入；旧 Lucky 和旧云环境保留恢复用途，不双写源码。
- 公开运行配置唯一入口为 `config/project.json`，修改后运行 `npm run configure`；不要手改生成的客户端配置或偷偷切回旧 AppID/环境。`npm run setup` 初始化本地，`npm run dev` 打开项目，`npm run demo:preview` 生成真实预览包。
- 新环境来自 LynkU AppID 的实际环境列表。旧微信身份和认证不能按昵称/邮箱前缀自动映射。平台确认及实际演示证据见 `docs/plans/refactor/status.md`，不把预览编译成功称为业务链路通过。

## 先建立上下文

- 项目是面向 UNNC 的社区微信小程序，包含帖子、评论、匿名互动、私信、通知和草稿。
- 文档入口为 `docs/README.md`。执行重构先读 `docs/plans/refactor/plan.md` 和 `docs/plans/refactor/status.md`，再按阶段读取产品、架构与治理规范。
- `docs/architecture/target-architecture.md` 描述目标，不能当作已实现；现有运行和验证方式见 `docs/DEVELOPMENT.md`，以真实 scripts/代码核对旧说明。
- 任务开始先检查工作区变更。保留用户与其他任务的未提交工作，不覆盖、不回滚无关改动。
- 历史聊天、设计草稿及 tickets 是背景资料。当前用户要求、实际代码和行为测试共同确定本次范围；发现冲突时明确指出，不默默改变产品行为。

## 必须保留的产品行为

- 微信是基础身份：启动后自动通过可信微信 OPENID 识别/建立账号，不要求用户在微信登录与游客登录之间选择。未完成学校邮箱认证的微信账号在产品中称为游客；邮箱认证是权限升级，不是另一套登录。
- 用户已授权恢复学校邮箱认证，使用已测试的 mail.oikoss.cc 与 noreply@oikoss.cc；在验证码和账号绑定验证完成后开启，当前实际开关以代码/云端为准。新登录用户可以浏览，已有认证用户权限保留，不能绕过服务端认证；密钥不得写入仓库。
- 区分微信身份尚未取得（加载/失败）、微信游客（未认证）和已认证账号；游客身份与匿名发言分开。保留用户明确要求的已有账号、微信关联和认证状态；重复账号可归并到已认证主记录，不按昵称或邮箱前缀硬编码认证白名单。
- 匿名作者真实身份由服务端保护，公开响应必须使用显式字段映射；客户端隐藏字段不能代替服务端保护。
- 登出和页面销毁必须终止对应轮询，并防止迟到响应恢复私有状态。
- 完整权限及有意修正以 `docs/product/behavior-contract.md` 为准。不要把现有权限缺口、消息遗漏或会话串读当作必须保留的行为。

## 实现规则

- 新业务模块使用严格 TypeScript；迁移期间允许旧 JavaScript 存在，不能只改扩展名并用 `any` 消除错误。
- 外部输入先按 `unknown` 处理并验证；数据库记录、API DTO 和页面状态使用各自类型。不要把私有数据库模型直接作为公开接口。
- 业务规则不直接依赖 `wx`、CloudBase SDK、环境变量或全局应用状态，通过窄接口注入这些能力。
- 页面负责交互和渲染，业务模块负责流程，适配器负责网络/存储。跨业务模块仅通过公开接口访问。
- 按 `docs/architecture/module-boundaries.md` 执行唯一数据写入所有权和跨模块用例编排；按 `docs/architecture/contracts-and-data.md` 处理协议、并发、事件与迁移。
- 依赖方向必须单向；不建立笼统的共享业务 `utils`。只抽取已经明确共享且具有稳定语义的能力。
- 对可重试写入说明幂等语义，对分页说明稳定排序，对并发计数说明事务边界。
- 服务端权限必须独立成立，不能信任客户端传来的用户身份、角色、认证状态或所有权。
- 遵循当前代码风格。避免将全项目格式化、目录移动、行为修改混在同一批变更中。
- 具体 TypeScript、状态、错误、依赖与评审规范见 `docs/governance/engineering-standards.md`；规范必须逐步成为可执行检查，不只停在文档。

## 验证和完成条件

- 当前可用完整检查：`npm run check`。不要把目标文档里尚未实现的命令报告为已运行。
- 当前源码入口为 `apps/miniprogram/` 和 `apps/cloudfunctions/`；构建/检查在 `tooling/`，运维迁移在 `scripts/`。微信开发者工具仍打开仓库根目录，只上传 `dist/cloudfunctions/` 构建产物；禁止恢复云函数源码 `utils.js` 副本。
- 业务重构验证行为、权限、响应及副作用；不能只靠源码正则匹配证明正确性。
- 新增构建步骤后检查真实产物，尤其是每个云函数独立上传时的依赖完整性。
- 影响设备生命周期、页面布局或云端事务时，明确哪些本地验证已完成，哪些还需要开发者工具/隔离云环境验证。
- 修改接口、数据模型或发布方式时，同批更新契约、迁移说明及必要的架构决策。
- 完成报告写清变化、验证结果和剩余限制，不把“文档已写”“骨架已建”称为迁移完成。
- 任务进度落在可检查的代码、测试和简短文档中，避免依赖某一聊天的记忆。
- 使用 `docs/governance/quality-gates.md` 的 G1–G4 和 A01–A15 记录证据；R0–R7 的本地完成不等于 R8 平台/上线验收。

## 自主执行与长期维护

- 用户已授权在既定边界内自行选择实现细节。普通库版本、函数/文件拆分、测试和 bug 修复不反复请求确认；有意义的方向变化按文档治理记录 ADR。
- 用户明确授权对当前原型进行破坏性重构：可以直接替换目录、接口、数据结构，删除废弃函数、兼容代码和原型冗余数据；不承担旧客户端兼容义务。现有已认证账号及用户指定账号的保留要求仍有效。涉及数据清理先验证备份与保留对象，随后自主执行，不为已授权操作重复请求确认。
- 根本产品范围、外部发布环境或破坏性真实数据操作不从“重构”两字推断；以当前任务明确范围与已有授权为准。
- 每个切片完成后更新唯一状态文件 `docs/plans/refactor/status.md`，记录证据、兼容层退出条件、下一步和真实阻塞。恢复工作先核对状态与代码，不重做已完成阶段。
- 不再生成同义计划替代实施，不创建无人使用的抽象/空包，不以测试数字、文件数量或形式化目录作为完成指标。
- 历史 docs/design、research、tickets 是背景。文档整理遵循 `docs/governance/documentation.md`；发布准备遵循 `docs/runbooks/release-and-operations.md`。
