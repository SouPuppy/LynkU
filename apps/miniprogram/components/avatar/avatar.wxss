/* avatar styles */
.avatar { border-radius: var(--radius-round); overflow: hidden; flex-shrink: 0; }
.avatar-sm { width: 56rpx; height: 56rpx; }
.avatar-md { width: 80rpx; height: 80rpx; }
.avatar-lg { width: 120rpx; height: 120rpx; }
