# FIX-003: Session/auth consolidation

| Field | Value |
|-------|-------|
| Status | DONE |
| Type | feature |
| Priority | high |
| Parent | EPIC-004 |
| Created | 2026-07-23 |

## Scope

1. Strip re-exports from `services/auth.ts`: remove `getCachedProfile`, `getOpenid`, `isLoggedIn`, `logout`
2. Keep `ensureLogin()` in `auth.ts` (it's the only caller of users CF for login)
3. Update all 13 page/component import sites to use `session` directly for cache ops and `auth` only for `ensureLogin`
4. Extract `isLoggedIn()` guard pattern into `utils/guard.ts` as `requireLogin()` helper (redirects to login if not authed)
5. Implement `scrollToBottom()` in `subpkg-chat/pages/chat/chat.ts` using `scroll-view` + `scroll-into-view`

## Import Map

| Before | After |
|--------|-------|
| `import { isLoggedIn } from 'auth'` | `import { requireLogin } from 'utils/guard'` |
| `import { getOpenid } from 'auth'` | `import { getOpenid } from 'session'` |
| `import * as session from 'session'` + `import { logout } from 'auth'` | `import * as session from 'session'` |

## Files

- `services/auth.ts` (strip re-exports)
- `utils/guard.ts` (new: requireLogin helper)
- `pages/index/index.ts`
- `pages/messages/messages.ts`
- `pages/drafts/drafts.ts`
- `pages/notifications/notifications.ts`
- `pages/profile/profile.ts`
- `pages/settings/settings.ts`
- `pages/my-posts/my-posts.ts`
- `pages/post/post.ts`
- `pages/login/login.ts`
- `subpkg-chat/pages/chat/chat.ts`

## Covers

- W-001: Auth/session import inconsistency
- W-003: isLoggedIn guard duplication
- N-002: scrollToBottom stub


## Resolution (2026-07-23)

实现已落地；`npm run check`（strict typecheck、云函数语法检查、10 项回归测试）通过。
