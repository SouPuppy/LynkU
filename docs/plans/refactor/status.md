# 重构进度与证据

更新：2026-09-21。R0、R1 已本地完成；R2–R6 正在推进；R7 工程治理进行中；R8 已开始局部平台验证，尚未完成。

## 上线保护 goal 实施中（2026-09-21）

- 2026-09-22：用户与认证列表的筛选、分页游标和选中用户详情已进入 `#users` URL；刷新、关闭详情和返回列表保留当前定位，畸形链接显示独立错误且不请求后台。管理端 TypeScript 构建通过（dist/admin-user-routing-typecheck.log），强制重建后静态网页已部署，当前入口引用 `index-CDtOstZh.js`（dist/admin-user-routing-build.log、admin-user-routing-deploy.log）。

- 2026-09-22：按管理端实际使用反馈补强“用户与认证”识别。批量列表新增稳定账号编号、业务角色及详情入口，学校邮箱在列表继续脱敏；受控单账号详情（仍要求 users:read）才返回完整学校邮箱、注册时间和能力限制，未增加 OPENID、匿名映射、私信或草稿访问。新增契约／投影验证拒绝畸形邮箱并证明详情不含 OpenID。完整 npm run check 通过317项（dist/admin-user-identification-check.log），admin 云函数部署后回读 Active/30秒，静态网页已部署且入口、JS、CSS、logo SHA256 均与本地产物一致（dist/admin-user-identification-function-detail.log、admin-user-identification-hosting-verified.json）。

- 2026-09-22：按用户追加产品定位，关于页、用户协议及社区规范统一强调“以校内信息展示与交流为主”及“禁止任何商业行为”，明确广告推广、商品／服务交易、付费引流、商业招揽与站外导流范围，覆盖帖子、评论、私信和资料展示。已同步生成文案；6项定向测试、产物一致性和文档检查通过。admin 云函数与静态网页已部署，四个线上管理端文件 SHA256 与本地产物一致（dist/admin-final-deploy.log、admin-final-hosting-verified.json）；最新小程序预览构建成功（dist/release-legal-preview.log）。本次为产品规则与展示修改，不代表已新增自动商业内容识别，也不代表已送审。

- 2026-09-22：按用户要求继续精简公开隐私文案，去掉Mailgun、CloudBase及云函数／数据库／区域接口等实现名称，用信息用途与邮件处理的日常表达替代，保留实际境外处理说明。同步生成公开正文与服务端摘要；6项定向文案与页面行为测试、生成产物一致性及文档检查通过，并随管理端正式更新到当前云环境；内容文本更新不改变已有用户的协议确认记录。

- 2026-09-22：后台内容管理新增受控恢复下架帖子。仅带关联治理案件的 hidden 帖子可操作；恢复必须提供理由，并在服务端重新核验帖子版本、原下架案件、作者未删除、分类状态及微信自动文本安全检查。申诉使案件重新打开、正文或版本变化、分类异常、检查拒绝或不可用均拒绝；事务内分类计数、幂等回执和审计确保重试或并发不重复恢复。完整 npm run check 通过316项，两个行为用例覆盖重复、冲突、安全检查失败与审计回滚（dist/admin-post-restore-check.log、admin-post-restore-test.log）。admin 函数已部署并回读 Active、30 秒超时；管理网页已部署，入口、JS、CSS、logo 四个线上文件 SHA256 均与本地产物一致（dist/admin-post-restore-function-detail.log、admin-post-restore-hosting-verified.json）。未在真实数据库执行恢复，仍需隔离环境以两账号和真实安全接口验证该写入流程；本条不把代码/静态部署当作真实内容安全接口通过。

- 2026-09-22：设置与权限接入真实协议正文、公开联系信息、备案展示状态、版本/摘要/生效日期及网页对管理云函数版本核对。四份文档使用 shadcn Sheet 与 settings?document 深链接；读取失败和不一致独立显示，不将管理函数版本冒充 identity/users 或微信正式发布版本。文案由既有唯一构建链生成管理端产物；新增生成漂移失败样本、严格 manifest 日期/缺失校验。完整 check 通过 313 项（dist/admin-legal-settings-check.log），随后扩展的 6 项法律构建/契约测试通过。admin 和 /admin 网页已部署，四个线上文件 SHA256 与本地产物一致（dist/admin-legal-settings-hosting-verified.json）。真实 CLI 管理身份本次取得成功，12 项只读调用全部通过，覆盖新 readLegalManifest 与总览/内容/用户/分类/案件/两种任务/审计/成员（dist/admin-read-smoke.json、admin-legal-settings-smoke.log）；不等于浏览器登录验收。浏览器清单能发现后台，但绑定该标签仍超时，实际点击及视觉验收未完成。完整后台仍需继续成员加入、内容处置/恢复、权利请求执行及全链路设备/浏览器验收，本次不标记总目标完成。

- 2026-09-22：按用户最新规则重构匿名私信：每次新发起带独立 initiation_id，原请求重试复用；同一来源同一双方再次发起仍分离。实名资料入口读取发起者匿名模式；消息列表明确恢复既有通道，不受后续匿名开关影响。匿名目录只公开 thread_id，移除来源及可跨通道关联的账户对屏蔽 ID/查询接口；消息 ID 不再以匿名账号作公开哈希输入，接收方不收到对方原始 msg_id。历史/同步/已读仍按独立通道，最终发送事务重验屏蔽。旧请求格式直接拒绝，既有账号/实名会话不改。结构图与产品规则在 behavior-contract.md，协议及切换边界在 contracts-and-data.md。完整 npm run check 通过 313 项（dist/anonymous-check.log）；同来源多通道、重复请求、历史/未读/回执、越权、屏蔽最终写入和客户端身份模式测试通过。微信真实 preview 成功（dist/anonymous-preview.log）。线上只读核对匿名 messages/conversation_entries 均为 0，未清理真实数据（dist/anonymous-cloud-inventory.json）；messages 云函数已部署成功（dist/anonymous-messages-deploy.log）；客户端需使用本次新版包，未提交微信正式发布。本轮两账号真机往返及真实云事务冲突尚未执行。

- 2026-09-22：分类新增接通真实管理接口：服务端生成 ID、事务内权限/唯一名称/100 项上限、幂等回执与审计；旧 categories 创建和 seed 路由退出。npm run check 通过 311 项；categories、admin、静态网页部署成功，核对分类唯一索引。备份校验后初始化 category_catalog/total=5，原五项分类保持不变（dist/category-catalog/2026-09-22T11-08-04.609Z）。未创建测试用真实分类，新版浏览器交互尚未实测。

- 2026-09-22：运行任务新增所有者专属operations:retry，详情中填写原因、核对尝试次数与重试版本后安排原任务重试。事务内重验权限、版本、来源账号/公开内容与已有lifecycle状态；失败pending或过期processing才允许，保持原任务及尝试次数，推进retry_revision并写回执/审计。重复请求复用回执，响应不明保留原请求；页面卸载不恢复旧状态，回执明确只代表scheduled。完整check通过308项（dist/admin-operation-retry-check.log）；随后追加资料投影与closing账号用例，3项实际适配器合成事务测试通过，含并发重复/冲突、租约保护、来源删除、撤权、审计故障回滚。admin与网页已部署，线上四文件SHA256一致；真实读取comments/users均Active，notification-outbox/profile-outbox定时器均为每分钟。没有对真实任务执行重试，浏览器和真实云并发验收未完成；未迁入统一lifecycle的账号及迟到投递整体治理仍须后续完成，不能扩大为注销验收。

- 2026-09-22：消息与运行新增真实通知/资料投影任务列表和详情，按类型/状态筛选、(created_at,_id)稳定分页，URL保存筛选/任务编号/当前位置。逐请求operations:read，只读白名单集合；DTO只含状态、尝试次数、时间、脱敏错误，任意原始错误映射UNCLASSIFIED_ERROR，不返回正文/收件人/验证码/身份映射。保留pending计数并独立展示列表错误；已完成与历史错误分开，不伪造邮件/审核/清理统计。完整check通过306项（dist/admin-operations-check.log），53条同时间翻页、跨类型/状态游标、隐私/错误过滤、租约必填等验证通过；4个outbox索引已应用读回，admin与网页部署成功，线上四文件SHA256与本地产物一致（dist/admin-operations-hosting-verified.json）。CLI再次未取得Web身份而返回AUTH_FAILED，真实登录后查询和浏览器交互仍未验证；受控人工重试和其他运行数据源尚未完成。本批不修改现有调度器、业务任务或真实用户数据。

- 2026-09-22：社区治理列表改为严格items/nextCursor协议，默认待处理，支持状态/帖子或评论/目标编号筛选，按(updatedAt,_id)倒序分页；申诉复核标记不带正文/举报人身份，筛选/分页/详情保存在URL，结案刷新并保留筛选。客户端与服务端同时退出旧cases数组，不设兼容层。完整check通过303项（dist/admin-case-list-check.log），新增56条同时间翻页、跨筛选游标拒绝、隐私投影与畸形记录验证。4个governance_cases索引应用读回，admin部署成功并读回Active，静态后台部署且入口/JS/CSS/logo四文件SHA256一致。本次部署后CLI session连续返回AUTH_FAILED，不能将CLI作为可靠Web登录身份，也不能因此判定浏览器登录故障；当前SDK确实从调用上下文读取身份。本轮真实登录后的新列表查询/分页及浏览器点击尚未验证，未降低授权要求来让检查通过；整体后台目标仍未完成。

- 2026-09-22：按用户要求正式整理小程序关于与公开文案（A03/A11/A15，G1/G2）。设置仅留“关于 LynkU”，关于页集中简介、版本、三份文档及quin@asro.cc；去掉复制按钮、每页重复入口、私人姓名、旧邮箱、未核实备案及占位文字。公开正文按当前功能重写；内部保留期限留在文档附录，不进入客户端。生成器采用公开变量白名单，个人主体资料即使存在也不打包，备案需核实标记与格式通过才展示。静态阅读页不调用协议确认，实际同意／写入屏障仍为独立未完成事项；ADR003记录边界。完整npm run check通过301项（dist/about-check.log），5项文案与页面行为回归通过，UTF-8／常见乱码／旧邮箱／占位扫描无命中；真实微信预览构建成功（dist/about-preview.log、dist/demo/preview.png）。自动化端口未能连接，未取得本次页面截图或真机排版证据；未部署云函数或送审，协议确认服务后续发布需同步新摘要，不伪造用户接受记录。

- 2026-09-22：后台操作记录退出最近50条截断，新增严格DTO、按(at,_id)倒序游标分页及动作/操作者/目标精确筛选；shadcn列表与只读Sheet显示理由、结果、请求与版本，筛选/页位置/详情进入URL。缺失字段显示未记录，不暴露举报人或匿名映射。新增readAudit逐请求audit:read鉴权，未提供旧events协议适配。完整check通过300项（dist/admin-audit-check.log），新增57条同时间分页、游标筛选绑定、投影隐私及异常验证。发现举报创建审计时间原为字符串，governance统一写数据库Date；迁移工具默认dry-run、备份校验、条件更新及检查点，真实库核对0条、无需改数据。5个审计索引已应用读回，governance/admin及静态后台部署成功；部署初期CLI身份未就绪，随后明确环境重新验证九类读取全部通过，审计筛选/NOT_FOUND/INVALID_INPUT真实返回通过（dist/admin-audit-cloud-smoke.json）。浏览器连接超时，本轮没有新增页面点击验收；时间范围筛选、完整运行任务处置、成员新增等完整目标仍未全部完成。共享工作区另有协议配置及版本提交，未覆盖它们，本次检查证据仅对应本批验证时源码。

- 2026-09-22：帖子详情同样经governance服务触发目录模块加载失败，已随设置页修复统一为明确文件引用，不增加兼容入口。客户端回归加载器移除Node式目录回退，实际执行settings/legal/reports/post模块注册，并验证帖子详情读取当前DTO、请求失败后重试及匿名字段保护。完整`npm run check`通过298项；微信实际`npm run demo:preview`成功（dist/page-load-fix-check.log、page-load-fix-preview.log）。当前版本仍为0.2.0，无账号或云数据变更；本轮未取得真机页面运行证据，编译成功不代替设备验收。

- 2026-09-22：用户提供设置页白屏日志，确切异常是`generated/contracts.js is not defined`，而非Skyline提示。修复settings/legal/governance服务及reports类型引用为明确的generated/contracts/index，并修订法律文本生成器中的同类入口。新增check-client-imports.js并纳入typecheck:client（亦进入check/CI），拦截TS能解析而微信运行时不支持的目录导入。微信实际preview通过（dist/settings-white-screen-preview.log），typecheck:client与check:runtime-config通过。自动化端口尚未连接成功，未冒充设置页运行时自动巡检通过；以用户日志定位、源码修复和实际重新编译作为当前证据。

- 2026-09-22：举报人对已结案举报提出一次申诉的链路已实现：可信本人身份、事务内归属/版本检查、固定请求重试回执、保留原结论/理由、重新进入open案件；不自动恢复任何内容。小程序举报结果页新增输入/提交/重试，管理详情展示原决定和申诉理由，现有结案流程提供复核结果。governance_cases新增updatedAt/_id索引并读回；管理列表按更新顺序显示。完整check通过296项，governance/admin与静态后台部署成功，实际微信preview成功（dist/report-appeal-check.log及appeal-*-deploy/preview.log）。此为举报人异议复核，不代替被处罚作者的申诉、账号限制通知、内容恢复或完整治理生命周期；这些仍未完成。

- 2026-09-22：用户明确确认当前线上后台“人工检查过OK”，记录为用户提供的页面验收，不再重复浏览器巡检；不能将此扩大成未经说明的并发/匿名越权/真机业务验收。已启动的云端只读检查完成：session、overview、listPosts、listUsers、listCategories、listCases、listOperations、listAudit、listMembers全部返回成功并通过对应结构检查，总览四项均available；证据dist/admin-read-smoke.json，工具tooling/admin-read-smoke.mjs。该检查使用有效所有者CLI身份，不修改数据，不代表普通身份越权测试。当前整体goal尚有申诉、完整运行处置等未完成要求，保持进行中。

- 2026-09-22：按用户最新“快速解决发布阻塞”暂缓操作记录分页扩展，退出本轮未完成的接口变更，保留已部署的最近50条查询。修复reports.wxml第15/16行表达式中的HTML实体&amp;，改为微信表达式原生&&；扫描小程序WXML无同类实体遗留。npm run demo:preview实际微信编译成功，AppID wxba2bcb0c71a5f33d，产物dist/demo/preview.png与preview-info.json，日志dist/release-preview-fix.log。后台按用户要求复用根assets/logo.png，登录页、侧栏及favicon统一，Vite直接导入生成带hash资源，无手工复制第二份图片；网页生产构建和静态托管部署成功（dist/admin-logo-build.log、admin-logo-deploy.log）。本条证明修复编译及更新品牌图标，不代表未完成的申诉/完整上线保护已完成。

- 2026-09-22：账号限制配套部署完成。新增只读快照工具snapshot-restriction-release.mjs（按_id分页、数量核对、本地原始备份及SHA256、明确10000条上限）；真实快照在dist/restriction-release/2026-09-22T09-54-11.195Z，4个用户保留2个已认证、4条带revision帖子，评论/私信及相关计数集合为空，5分类；未修改任何业务数据。posts/comments/messages旧代码分别下载备份，随后三函数部署成功并读回Active、30秒、Nodejs16.13；再部署admin与静态网页（dist/restriction-*-deploy.log）。因此此前“限制切片尚未部署”已被本记录取代。真实CLI可读用户限制，唯一管理员自撤权请求返回CONFLICT（dist/restriction-cloud-smoke.json），未执行真实用户限制或撤权。CLI调用session确认其当前携带有效platform-owner身份，所以伪造请求字段后得到NOT_FOUND不是“无身份被拒绝”的证据；普通Web身份/匿名越权和真实事务并发仍需单独验证。快照为按表读取的发布备份，并非跨集合事务快照，不声称支持任意并发写期间的一致恢复。

- 2026-09-22：账号限制切片已本地实现。用户详情接readUserProtection/updateUserProtection，按posts/comments/messages分别限制1小时/1天/7天/30天或解除，显示当前期限、原因、版本、原请求重试和真实回执。identity唯一写users.restrictions，缺字段表示从未受限，不改已有邮箱、verified或微信关联；管理授权、限制版本、回执和审计同事务。入口仅限制发帖/修改、评论创建和私信发送，最终内容/评论事务重新读取同一users记录；消息适配器新增发送账号定位、事务内验证真实owner/verified及限制。共享account-capability是纯能力策略，没有数据写入。完整npm run check通过295项及云包构建（dist/account-restrictions-check.log）；新增验证覆盖期限边界、能力隔离、认证保留、幂等回执。**本切片尚未部署**：须先核对线上posts/comments/messages的配套协议与数据，然后一起部署写入屏障，最后更新admin与网页；不能提前开放只对部分写入生效的管理按钮。真实云并发/设备验证、限制结果通知/申诉、统一lifecycle屏障仍待完成。

- 2026-09-22：设置新增真实管理成员列表与权限/撤权Sheet，接入listMembers/updateMember；独立DTO不返回Web UID或业务身份映射。服务端只允许settings:write，事务复核操作者和目标记录，以当前版本、请求指纹及原子审计回执处理重复提交；禁止修改自身资格，保证另一位有效所有者保留管理入口，独立平台账号不能降为业务角色。页面展示变更前后、原因、版本冲突及原请求确认，成员查询超过100明确失败不静默截断。完整npm run check通过293项及独立云包检查（dist/admin-members-check.log）；新增测试证明自撤权拒绝、重复回执不重复版本/审计和平台角色约束。真实云事务并发冲突仍待平台验证；新增管理身份的受控关联、账号限制与申诉未在本批完成，不将此切片当作全部后台验收。

- 2026-09-22：管理网页统一请求加入会话代际屏障，退出或AUTH_FAILED/FORBIDDEN后立即卸载受保护页面，禁止同会话继续发请求并丢弃已派发的迟到响应；重新登录等待前一次SDK退出结束。后台获得焦点及前台每60秒重新核对管理资格，离开页面取消检查，资格版本变动更新会话。viewer治理入口显示无权限而不请求受限接口。登录页显示会话恢复/失效原因，网络退出失败不产生未处理拒绝。深色主题恢复本地选择和系统默认。两项针对异步旧响应与清理监听失败的必要测试通过，管理端生产构建通过；新版静态网页部署成功（dist/admin-session-build.log、admin-session-deploy.log）。SDK底层已发网络请求不能撤回，本批保证其响应不能恢复失效UI，不声称撤销已提交服务端写入；真实浏览器撤权和全后台验收仍待完成。

- 2026-09-22：完成实际管理员网页更新。25个schema集合已逐项设置并读回ADMINONLY、补齐缺失集合与索引，无未解决冲突；保留已有数据及旧索引。发现admin_members原为PRIVATE，已立即收紧并读回。结构备份和应用结果在dist/admin-cloud-schema/cloud1-d7gifgdpb8ad0ab8e/applied.json（completed=true）。admin旧代码已下载备份，随后admin与新governance云函数均部署成功；shadcn网页已上传/admin，线上index.html、JS、CSS分别下载核对SHA256与本地产物一致。相关日志为dist/admin-function-deploy.log、governance-function-deploy.log、admin-hosting-deploy.log。沿用整批已通过的289项完整check，不重复运行未变更业务的全套测试。浏览器工具打开页面超时，因此本次不宣称登录后八模块及真机验收通过；成员管理、账号限制、申诉等完整目标仍继续实施。新增provision:admin-cloud默认dry-run，--apply显式应用；不是重复迁移或清空库。

- 2026-09-22：开始实际云端切换。重新读回目标环境7个函数，governance尚不存在；已确认5个真实分类缺managementRevision。新增migrate:admin-categories工具（默认dry-run、集合计数核对完整快照、本地备份摘要、逐条条件更新、检查点与最终读回；不更新原字段或已有revision）。先备份云categories代码与5条分类，再部署已检查的categories云包关闭旧update动作；真实部署成功且Active读回，ModTime为2026-09-22 17:21:34。随后仅为5条记录初始化managementRevision=0，重复dry-run确认缺失数0，没有修改账号或认证。证据位于dist/admin-category-migration/cloud1-d7gifgdpb8ad0ab8e与category-deploy/category-migration-apply日志。整批npm run check通过289项测试和产物检查（dist/admin-release-check.log）；不代表其他功能已云端部署，admin与governance新版本仍待配套集合／索引／权限和平台验证。

- 2026-09-22：打通用户侧举报结果列表，governance.listReports按可信账号过滤并绑定账号游标，返回本人回执／结论／理由，不返回目标、举报说明或身份映射。小程序设置新增“我的举报与处理结果”页面，支持刷新／翻页／空与错误状态，隐藏、销毁和换账号后不接受迟到结果，不持久缓存私人列表。复用现有reporterAccountId/createdAt/_id索引及字符串日期。服务端构建、小程序严格类型与云严格检查通过；账号隔离、53条同时间分页及私有字段剥离的针对性验证通过。未部署或真机验证；该入口是主动读取回执，尚未实现主动通知／申诉，仍须完成这些功能后才能称治理闭环交付。

- 2026-09-22：首页空白排查（A05/A15，局部G3/G4）：开发者工具实际复现首页error、posts为空；云端posts.list返回3条记录但缺少nextCursor，当前客户端parsePublicPostPage拒绝旧分页格式。核对LynkU环境列表后，从dist/cloudfunctions仅更新posts至现有严格TS/游标实现，部署成功；真实limit=1两页读取通过当前契约且无重复，重新启动首页为loaded/3条/error为空，并检查截图dist/demo/home-final.png确认帖子可见，没有创建测试帖子或修改既有记录。tooling/demo-smoke.mjs补共享响应校验和真实游标翻页检查。帖子相关16项回归、8份独立云包验证通过（dist/home-debug-tests.log、dist/home-debug-artifacts.log）；完整npm run check在当时工作区的后台case-decision.ts缺少removeComment实现处失败（dist/home-debug-check.log），未修改其他任务的治理代码。未发布客户端或其他云函数；手机冷启动仍需补验，无新增兼容层。

- 2026-09-22：评论违规处置接入readComment预览及remove_comment结论。评论预览返回匿名安全DTO与绑定评论内容／来源／匿名状态的版本摘要；删除父帖不提供评论正文。提交在同一事务复核当前摘要，清空正文／作者快照、保留回复关系、减少帖子评论数、推进comment_counters与comment_changes删除事件，并写案件结论／回执／审计。实际适配器合成测试证明重复请求只减一次、只生成一次同步事件，审计失败全部回滚；服务端、管理端构建与云严格检查通过，未重复全套。尚未部署；需要继续补通知／申诉与恢复、迁移、平台验收，完整目标仍进行中。

- 2026-09-22：治理新增hide_post结论，案件侧栏读取并展示当前帖子正文／revision，仅可下架当前published版本；服务端治理通过窄content端口执行状态转换，原子写hidden、revision+1、分类计数-1、closed案件、回执和审计。下架不恢复或补发安全检查失败内容；现有作者编辑拒绝hidden，通知读取按源状态隐藏旧预览。针对实际管理适配器验证同请求只扣一次计数、审计故障整体回滚；服务端／管理端构建及云严格检查通过。本地新增能力未部署；评论违规处置、恢复与申诉、通知和生命周期清理仍需继续，未宣称整体治理完成。

- 2026-09-22：治理接入真实案件详情与“未发现违规／重复举报”结案，新增shadcn案件侧栏、理由、版本、回执、冲突和原请求确认；详情深链`#governance?case=...`。治理写入复用从分类提取的事务身份复核适配器，案件／幂等回执／审计同事务提交。本人readReport按可信业务账号提供结案结果，跨账号NOT_FOUND，普通详情不带举报人身份。该批服务端／管理端构建与云严格检查通过，针对结案幂等／身份保护及共享授权适配器的必要行为验证通过；未按每个编辑重复全套。未部署；违规下架联动、结果通知／用户入口与申诉、完整分页仍待继续，不能称治理模块完整。

- 2026-09-22：按用户要求调整为成批实现、集中验证，不再每个小改动重复完整检查。帖子详情接入真实admin.listComments，评论用例复用comments投影后显式删除身份字段，保留回复关系；游标绑定帖子，查询前后核对父帖仍可读；新增本地post_id/created_at/_id索引。该批完整check通过285项既有测试（不代表新增评论并发场景已有专项测试），未部署。
- 同批将治理／运行／审计／设置四个占位页面替换为真实查询或当前可信会话／构建环境信息，统一shadcn列表与状态。案件reasonCode、审计caseId目标映射按真实写入修正；三个读取响应补运行时字段校验。案件和审计目前明确显示最近50条，尚无全量分页／处置；运行仅pending计数，设置只读而非成员管理完成。新增UI批次管理端严格类型与生产构建通过（dist/admin-build.log），没有为每个页面重复全套检查。整体后台仍未达到可发布验收；案件处置、成员管理、迁移、云端及浏览器验证继续推进。

- 2026-09-22：帖子详情已从列表接入真实 `admin.readPost`，每请求核对content:read；删除／不存在统一不可读，ID错配失败，显式正文DTO不带匿名作者或私有字段。新增shadcn详情Sheet、纯文本正文、状态／版本、刷新、错误及加载状态，`#content?post=...`可直接打开并刷新恢复。新增行为测试验证匿名脱敏、删除／错配／非法ID拒绝及正文保留为文本；完整 `npm run check` 为285/285通过（dist/admin-check.log），含管理端构建和云包。本次未部署或浏览器验收；评论语境、处置动作及远端变更自动刷新仍未实现，详情当前为主动刷新。

- 2026-09-22：内容管理接入真实帖子列表与shadcn状态标签／表格／搜索，支持published、hidden、历史flagged、deleted四状态及稳定前后翻页。共享契约从unknown校验，服务端绑定筛选游标并验证结果范围；匿名非布尔不再当实名，匿名真实作者及快照不进入DTO。删除记录不展示原标题／作者，拒绝删除标题搜索。3项行为测试证明57条同时间分页无重漏、跨筛选游标拒绝、错误数据库范围拒绝、匿名与删除脱敏。最新完整 `npm run check` 为284/284通过，管理端生产构建及云包通过（dist/admin-check.log）。未部署或浏览器验收；内容详情／评论语境／分类筛选及处置动作仍待接通，不能称内容模块完整交付。

- 2026-09-22：分类列表改为严格带managementRevision的管理DTO，接入shadcn Sheet编辑侧栏（名称／描述／排序／启停、必填原因、前后预览、保存回执）。页面按categories:write显示编辑，服务端仍独立鉴权；冲突保留输入并要求刷新，未确认结果锁定原requestId与请求内容重试，卸载后丢弃迟到界面结果。旧categories.update云动作／权限在本地退出，新建与seed写入初始管理版本。完整 `npm run check` 为279/279通过；后增2项契约行为测试通过，证明隐藏分类／版本保留、私有字段剥离、未迁移版本与畸形回执拒绝。尚未部署或浏览器验收；历史分类版本初始化、回执清理、真实事务／唯一名称冲突仍需验证，分类创建网页仍未接入。

- 2026-09-22：新增content分类版本修改用例与真实 `admin.updateCategory` 路由，管理适配器在同一事务重新读取可信成员、业务账号和生命周期，核对 categories:write；原子写分类、actor作用域幂等回执及审计。支持修改名称／描述／排序／启停，理由必填；旧版本／同请求换正文冲突，保留服务端实时post_count。3项业务测试证明并发只有一次修改、回执重放、计数保护、审计失败回滚和撤权拒绝。完整 `npm run check` 为277/277通过，含新增云源码的独立打包；随后2项实际适配器合成数据库测试通过，证明事务内撤权复核与可信操作者／单次审计。新私有回执集合进入schema。未部署；分类managementRevision初始化、旧categories.update写入口退出、回执保留清理、编辑UI与真实事务验证仍待继续，不能称分类管理完整交付。

- 2026-09-22：用户与认证列表接入真实 `admin.listUsers`，使用 shadcn Table／Badge／Tabs／Input，支持昵称字面搜索、认证筛选、前后翻页、刷新、加载／空／错误状态，并丢弃卸载或请求替换后的迟到结果。共享请求／响应契约从 unknown 校验，服务端每请求复核 users:read，使用筛选作用域的 `(created_at,_id)` 排他游标和 limit+1；邮箱脱敏且显式排除微信身份。新增行为证据遍历53个同时间账号无重漏，拒绝跨筛选游标、错误查询范围、畸形认证及页边界。完整 `npm run check` 通过274/274测试与全部构建检查（dist/admin-check.log）。users分页索引已进入本地schema，尚未应用或验证云端；昵称搜索性能、浏览器交互、用户详情及限制写入仍待完成，未部署该切片。

- 2026-09-22：继续统一官方 shadcn/ui 页面，总览使用 Card／Skeleton／Button，分类独立为 feature 并使用 Table／Badge，补齐加载、空列表、错误重试及卸载后的迟到结果丢弃。总览读取真实当前计数，按能力分别授权、隔离单项故障，缺权限／不可用均不显示为零；公开帖子只计 published。新增两项行为测试覆盖故障隔离、越权计数不查询、合法零值及非法计数拒绝。动作策略检查改用 TypeScript 语法树识别 action 分派，避免将内部指标 switch 当成云动作。完整 `npm run check` 已通过，271/271 测试、管理端生产构建及独立云包验证通过，日志 `dist/admin-check.log`。本次页面与总览接口尚未部署，浏览器验收、其余管理页面和写操作仍未完成。

- 2026-09-22：按用户指定 HTML 视觉参考，管理端接入官方 shadcn/ui new-york-v4 组件源码及 radix-ui；新增 components.json、路径别名和浅深主题变量。已使用 Sidebar、DropdownMenu、Breadcrumb、Input、Table 重建外框、登录输入与分类表格，支持折叠菜单和主题切换。组件源来自 ui.shadcn.com 官方注册表，未复制演示数据或宿主事件。管理端严格类型检查与生产构建通过；八模块完整业务、详情／操作、分页及浏览器验收仍待继续，不将 UI 基础切片当作完整交付。

- 网页管理后台的首条真实身份链已落地：新增 `apps/admin` 的 React／严格TS／Vite／Tailwind 工程与八个可深链导航入口，登录与会话恢复调用 CloudBase Web SDK；新增 `admin` 云函数、`admin_members` 私有集合和 ADR 004。服务端只从签名 CloudBase 上下文读取 Web UID，并在每请求重核预绑定、既有业务账号、active生命周期、学校认证、admin角色和固定能力；拒绝匿名／未绑定／撤权／关闭账号和浏览器伪造角色。当前页面不展示设计稿或假数据，查询和高影响操作尚待按模块接入。全套 `npm run check` 已实际通过，267/267 测试、8份独立云包、管理端类型检查与生产构建均通过；未部署、未建立真实首位绑定、未配置网页登录或静态托管，不能称为可登录后台或正式发布。
- 首发平台所有者已按用户最新决定真实启用：唯一 CloudBase 网页登录名为 `admin`，通过受控 `platform-owner` 记录绑定到其 CloudBase Web UID，不绑定、不伪造也不改动任何小程序用户或学校认证。`admin` 云函数已部署为 Active，环境网页域名已在安全域名名单；一次真实用户名密码登录及签名上下文 `session` 调用成功，匿名 CLI 调用仍明确返回 `AUTH_FAILED`。本地完整检查为269/269通过。当前仅完成会话与分类读取，其他后台模块仍待接入。
- 后台分类读取已接入首个真实业务模块：`listCategories` 只对已验证会话的内容查看能力开放，服务端按新索引稳定排序并显式投影分类字段，包含停用分类且不返回数据库私有字段。网页分类页显示真实加载／空／错误状态，不回退示例行。新增投影回归通过；本轮完整 `npm run check` 为268/268通过。该切片仍未包括分类创建、编辑、排序或停用写入。
- 静态托管发布链已固定为 `npm run deploy:admin`：先构建管理端，再以 CloudBase CLI 将产物安全上传至目标环境的 `/admin`，并逐项核对远端文件清单；不会清空或覆盖托管根目录的其他站点。2026-09-22 已使用已授权目标账号真实发布，入口为 `https://cloud1-d7gifgdpb8ad0ab8e-1492831971.tcloudbaseapp.com/admin/`；入口、CSS 与 JS 三个产物均由托管清单读回核对。CloudBase CLI 3.8.4 的子目录 `--verify` 错误报告文件缺失并回滚，发布脚本已改为安全备份上传后读取远端清单验证。默认域名对非浏览器请求带下载响应头是平台保护机制，正式公众访问仍需绑定自定义域名。
- 管理网页已纳入持续交付：`Verify` 的完整检查包含管理端严格类型检查与生产构建；新增 `Deploy admin` 工作流只针对 `main` 或人工触发，在 GitHub `production` Environment 审批后，以两项受保护 CloudBase API 密钥登录并运行同一 `npm run deploy:admin`。CLI 被锁定为本地精确版本，缺密钥或无环境权限会明确失败而不切换环境。尚未在远程仓库配置密钥／Environment，也尚未触发工作流，故没有远端 CD 成功证据。
- 运行时供应链复核：管理网页的生产依赖审计为0项；现有云函数运行时仍经 `wx-server-sdk@4.0.2 → @cloudbase/node-sdk@3.17.2` 带来4项high和1项moderate通报。修复建议要求切换到不兼容的Node SDK 4／旧wx-server-sdk，不在未做隔离云兼容验证时强行升级或用覆盖规则伪装解决；该项仍是上线阻塞条件。
- 用户明确追加完整网页管理员后台，覆盖历史“小程序内最小治理工作台、不做独立后台”的范围限制；ADR 003与产品范围已按最新决定修订。边界不变：浏览器不能直读私有集合、不提供全站私信／草稿／匿名身份调阅，所有管理动作仍经可信 Web 身份、服务端能力和所属业务模块。交接包已读取；当前仓库尚未具备Web身份绑定和管理API，后续先做真实身份闭环及apps/admin工程，不能用演示数据替代。
- 私信屏蔽已接入服务端写入屏障：以双方可信会话参与者生成受限的稳定pair键，普通与匿名会话均在发送事务前校验；屏蔽后不写消息、序号、目录或未读数，解除留tombstone而非删除屏障。客户端聊天页可执行屏蔽／解除，普通响应不返回对端或跨来源关联。265项本地回归均通过（含屏蔽写入前阻断和原聊天加载回归）；持久屏蔽状态列表、接收偏好及注销期关联仍待完成。
- 举报受理基础已实现：新增第七个独立 `governance` 云函数和受控 `governance_cases` 集合，`submitReport` 仅接受当前公开帖子／评论、严格限制目标／理由／说明长度，基于账号与requestId确定性幂等；事务内写入案件及最小审计，不回传举报者、目标作者或匿名映射。并发重试收敛、换目标／理由冲突、无效／不可见目标不建案件均有行为测试。当前仅是受理基础，案件查看、处置／结果、申诉、私信举报及客户端入口尚未完成；本地定向回归263/263和7份独立产物通过，未部署。
- 私信发送现已接入同一微信 `msgSecCheck` 适配器（scene=2）：在收件人查找、限流和任何消息／目录／未读写入之前完成检查；review/risky、畸形响应、权限／网络故障、超时均不提交。messages 源配置、部署清单均为 30 秒并声明该 OpenAPI 权限，防止 3 秒函数超时绕开检查。定向消息／审核测试通过，独立云函数产物验证通过；真实云端权限读回仍属G3。
- 最新工程门禁已完整通过：`npm run check` 为 260/260 通过，包含六个云函数的实际严格 TypeScript 编译、独立云函数产物、配置／文案／schema／动作策略及客户端检查。此前 users/messages 入口的真实类型诊断已经收口，未关闭 strict、未以 any 或排除入口换取通过。此为本地 G1/G2 证据；协议生命周期、举报／屏蔽／注销及 G3/G4 平台证据仍未完成，不能据此宣称可以送审或提交 0.2.0。
- 用户最新要求快速收敛到可发布，并授权完成后直接提交0.2.0；本轮按代码版本提交处理，正式微信送审／公开发布仍未授权。上一轮完成评论／帖子入口收口属有效进展；本轮转向实际协议确认能力，不继续泛化整理。
- 服务协议确认切片：identity新增acceptAgreement用例、users.acceptAgreement真实路由、legal页主动确认按钮；不预勾选，不以继续浏览表示同意，不改变认证或隐私目的授权。服务端核对当前terms/rules版本及摘要、正式生效日期，事务内检查account_lifecycle的当前账号／epoch／active并更新writeFence，同步记录两份agreement_assents；同一操作重试返回原时间，失败整体回滚，关闭账号不能接受。云端摘要由与离线正文相同源生成并进入独立包，不手写另一份版本。
- 本切片4项协议行为测试证明显式选择、草案／未来版本拒绝、版本变更、重复请求、账号关闭／绑定变化及事务回滚；全套npm test259/259通过，客户端类型与配置／动作／schema检查通过，完整check仍因users/messages既有入口类型问题失败。account_lifecycle迁移／初始化、首告知隐私挑战、写操作统一协议屏障尚未接通，故不能称CS01完成；当前配置draft，按钮不允许接受草稿，不伪造真实生命周期或同意记录。新增集合仅写入本地schema，未部署／迁移真实数据。
- 评论与帖子入口继续收口：读取／写入／状态事务分别装配明确的业务端口，接入SDK结果校验；缺失账号文档ID不能继续写入，评论计数与日期先验证，帖子详情管理员判定复用公共身份检查。评论通知生成／落库改用已有notifications模块，退出云入口内重复业务逻辑；补偿领取的通知从unknown进入校验。保留匿名映射、原子计数、幂等和失败不发布行为。
- 最新全云严格诊断中comments/posts及已完成common/database/categories/drafts均无错误，仅users/messages仍待收口。全套npm test仍255/255通过，含独立云函数产物验证；完整npm run check再次执行，仍因剩余入口真实类型错误失败（dist/community-safety-check.log）。测试夹具补真实账号文档ID及当前相对依赖加载，没有放宽生产校验。前轮分类／草稿适配为有效进展，本轮为评论／帖子生产适配进展；完整上线保护目标、云／真机验收未完成，未部署／送审／改真实数据。
- 最新全套npm test为255/255通过，含当前6份独立云包构建与运行检查；日志dist/community-safety-tests.log。它与下列完整严格检查未通过的结果分别记录，不等同正式平台验收。
- 分类／草稿入口已使用connectDatabase校验SDK结果并按CategoryStore／DraftStore显式装配；分类排序缺字段不再默认成0，草稿写入日期从已验证字符串转换。保留SDK已实现但类型遗漏的throwOnNotFound=false选项，使用明确交叉类型配置，不改缺文档语义、不增加any。common、database、categories、drafts在实际全云严格检查中均无诊断，剩余users/posts/comments/messages仍未完成。
- 数据库边界新增缺文档／畸形单记录、事务读取、列表和计数错误行为测试；身份及草稿页面73项回归通过，测试加载器按当前云入口正确解析相对依赖。前轮限流修复为有效进展，本輪为生产适配及类型收口进展。完整npm run check已运行，仍因其余四个入口的真实类型错误失败，日志dist/community-safety-check.log；不把此切片当作CS01—CS12全部完成，未部署或改真实数据。
- 最新公共工具切片：common/index.ts在实际全云严格编译中已无诊断；SDK结果经unknown验证，补偿任务列表／ID、领取／完成文档、重试计数和定时结果均验证，日志只写指定任务计数。限流不再吞errCode=-1当作缺记录，不接受错账号／动作、非整数计数、损坏或未来窗口，不重置后放行。已读取当前wx-server-sdk源码确认throwOnNotFound=false时缺文档返回data:null，测试使用该形状，另覆盖通用-1错误不写入。
- 最新验证：全套npm test为253/253通过；随后定时日志收窄后，公共工具／调度15项再通过。完整npm run check仍因各业务云入口实际类型错误失败，公共工具已退出错误清单，证据dist/community-safety-check.log。前轮授权修复为有效进展，本轮继续生产限流／补偿边界修复，无外部等待、部署或真实数据修改。账号生命周期、同意、举报／屏蔽／注销仍须实施，整体目标未完成。
- 用户已启动完整实施目标，本节覆盖下方“只做设计”的历史授权阶段。目标保留全部CS01—CS12与6个实施切片，不缩减为文案页面；不做人工预审、不增审核商／AI、不保留旧协议兼容。前次被中断的核对未改业务，本次已开始实际代码修改。
- 已落地公开规则首片：新增独立 legal 页面，用户协议／隐私政策／社区规范／关于均随包可离线阅读；设置、身份失败的我的页和登录页可进入，不要求先登录。展示用户原备案串和联系邮箱，支持复制及打开微信隐私指引，未猜填完整姓名。
- config/project.json 新增严格白名单legal配置；政策草稿／生效、版本、日期、主体、备案核验与服务提供方字段可验证。草稿可开发预览，未核实字段／遮盖姓名／不合格式备案／秘密或未知键不能直接变为active文本。文案由community-policies.md生成客户端文章及相同摘要的dist/legal/manifest.json，不维护两套正文；生成物一致性已接入check:runtime-config。
- 真实验证：6项配置／文案行为测试通过，客户端类型检查通过。完整npm run check已执行，类型、依赖边界、文档、配置和文案产物均通过，随后check:cloud仍查找已由共享任务迁走的common/index.js而失败，日志dist/community-safety-check.log；未恢复旧JS或绕过检查。云函数TS迁移由既有共享任务进行，实施后续先核对其当前状态。
- 自动检查增加实际有界执行：单段6秒、并发最多2、所有段共用20秒截止时间，每段仅一次；失败停止后续分段，超时或事件循环迟到的pass不能继续提交。时钟通过端口注入，微信适配使用Node单调时钟与可清理定时器。已接入的users/posts/comments本地部署配置统一30秒；未部署，不能当作云端已读回确认。未增加人工预审或外部服务。
- 本轮核对前次为有效进展（离线文案代码与行为证据已存在）。新增5项超时／并发／迟到结果测试，连同既有审核与发帖行为共10项通过；实际发帖用例证明超时后正文与分类计数均未写入。包构建和6个独立云函数产物验证通过。完整check再次运行，仍在旧common/index.js路径失败；另运行全套npm test，旧云入口测试仍读取已迁走的index.js而失败，详见dist/community-safety-tests.log，未称全套通过。分段回执／操作幂等预算、私信审核、去除旧flag路径、身份生命周期与G3/G4仍未完成。
- 后续发布切片：帖子创建／修改、评论创建必须得到明确clean=true，false拒绝、畸形结果不可用，不再保存新的flagged候选；修改失败保留原正文、版本和分类计数。昵称同样拒绝非布尔通过值。删除posts.flag动作和moderatePost直接恢复发布方法，普通／管理员均无法调用该旧动作；已有受限内容暂仍可读取本人视图／删除，存量处理留给直接切换迁移，不自动公开。保留删除的权限、事务和审计逻辑。
- 本切片16项相关行为测试通过，包括真实评论入口连接合成SDK时review/risky/畸形结果不生成评论、同步序列、计数或通知，以及帖子失败不写入／不覆盖和旧flag动作拒绝。6份独立云函数产物再次验证通过；完整check的类型／依赖边界／文档／配置通过，仍在既有common/index.js旧路径失败，未以局部通过替代完整验收。前一goal轮为有效进展（时限实现及测试），本轮继续修改生产代码，未部署或改真实数据。
- 验证链已修复旧路径：guest-session的合成云入口加载当前index.ts；check:action-policy显式通过tsx加载当前公共入口。73项身份／权限／页面回归恢复通过，全套npm test实际249/249通过（dist/community-safety-tests.log）。check:cloud不再扫描空的JS源码集合或用旧导出正则判断TS，改为读取实际云端tsconfig并执行严格编译器诊断，强制所有清单入口存在，检查语法、导入及类型。
- 最新完整npm run check仍未通过，但原因已从文件不存在转为实际云入口严格类型错误：隐式any参数、SDK响应联合类型未收窄等，详见dist/community-safety-check.log及dist/cloud-types.log。未禁用strict、添加any或排除云入口来获得通过。这是待完成的生产代码迁移工作，不是等待外部资料；后续继续收口云适配与上线保护业务。前轮为有效业务进展，本轮为验证工具修改及新行为证据，无真实云部署／数据操作。
- 公共授权入口开始实际收口：可信OPENID和外部事件从unknown验证，授权结果使用可判别类型；权限表只接受自有键，拒绝constructor／__proto__等原型属性。账号读取检查SDK响应、最多取2条识别重复、核对返回记录的OPENID，重复或错配不能静默取第一条授权；故障日志不输出原始错误正文。该检查不归并或删除真实账号，保留要求不变。
- 新增身份畸形／重复／错配、权限表原型名、可信OPENID类型3项行为验证；独立包夹具同步验证2条上限的实际查询。全套npm test为252/252通过，含独立云包运行；完整npm run check再次运行，仍有其他公共工具及各云入口未迁完的严格类型错误（dist/community-safety-check.log）。前轮验证链修改构成有效进展，本轮是生产授权代码及验证进展；整体CS01—CS12未完成，未部署／送审／改真实数据。
- 下一步继续identity的同意／生命周期／内部标识和提交屏障，再接举报处置、屏蔽与注销实际流程；本次页面不代表同意记录、注销或数据权利已完成。所有文案保持draft且未生效，未部署、未送审、未改真实账号数据；G3/G4和正式资料仍待后续证据。

## 上线保护完整设计（2026-09-21，历史设计阶段）

- 用户明确本轮只生成完整方案，稍后以 goal 实施；不反复询问普通决定。本轮未创建 goal、未新增业务代码、未部署／正式发布／提交外部材料。共享工作区其他开发任务的改动继续保留，下方 0.2.0 记录属于其实际开发状态，不能与本次设计混为已实现。
- 已形成 [产品与页面边界](../../product/community-safety.md)、[完整协议／隐私／社区规范及短文案](../../product/community-policies.md)、[技术契约与18集合清理／迁移](../../architecture/community-governance.md)、[ADR 003](../../architecture/decisions/003-community-safety.md) 和 [后续 goal 指令](../community-safety-goal.md)。文档分工明确，进度仍只维护本文件。
- 最新决定覆盖早期建议：**不做人工预审，不建持久审稿候选库／待发布队列，不等管理员批准，不接额外审核商或通用 AI；微信自动检查 pass 后在当前请求直接发布，review/risky 不提交、故障保留输入。** 人工只处理具体举报、申诉、隐私例外。响应丢失必须用原请求确认，不谎报未提交。
- **只保留一套新客户端／服务端协议，直接切换，无向后兼容、双写和旧接口 fallback。** 已有账号、微信关联、认证及内容按映射和备份核验保留，不因去兼容而清库。初始隐私时序、提交屏障、跨匿名来源屏蔽、注销重建及18集合／新增集合清理均有明确目标。
- 用户提供备案原文“浙CP备2026058998号-1X”，按已备案处理；只从微信后台核对显示字符串，不重办。拟公开联系邮箱为 zihannuo@outlook.com，完整运营者姓名和实际供应商合同据授权资料核对，不伪造遮盖姓名。现有学校邮箱认证继续保留。
- 官方接口约束已核对并入设计：完整文本分段、近两小时可信微信访问、未发布账号每日100次、逐函数openapi权限和真实超时读回。没有取得新一轮真实内容检查／云端／设备通过证据。方案复核修正了同意与更正冲突、关闭标记到期重建、注销绕过屏蔽、消息历史误套同步TTL、副本并发及同意记录唯一索引问题。
- 验证：本轮 `npm run check:docs` 通过；另对5份新增设计逐份检查本地链接、代码围栏和UTF-8替换字符，均通过。仅修改设计／规范文档，未运行本轮业务测试，不复用历史221/231测试数字作为新实现完成证明。下一步由用户启动 [实施指令](../community-safety-goal.md)，先核对0.2.0最新代码，再按既有R2—R8与CS01—CS12落实，不重新写同义方案。R8及外部公开准入未因此完成。

## 0.2.0 实施中（2026-09-21，最新开发授权）

- 用户授权不兼容旧客户端的工程架构重构，继续原生微信与 CloudBase，保留真实账号及认证；“500字”指回复建议篇幅，帖子仍保留 10000 字限制，未缩减产品功能。
- 已完成切片：评论创建/删除/历史/同步严格 TS；读取故障不重置序列；通知按当前来源状态清除失效内容预览；分类显式 DTO、确定性幂等初始化及统一禁用规则；管理员权限同时要求认证。
- 首页、搜索、作者列表统一使用纯 TS 列表控制器和绑定筛选条件的稳定游标；评论独立历史游标与同步水位，保留未加载父评论的回复，实时删除不会被迟到分页覆盖；页面隐藏/账号切换/销毁使旧响应失效。旧 offset 及重放全部评论历史实现已删除。
- TypeScript 已升级 6.0.3，移除旧 Node 解析模式与 baseUrl；全栈类型检查通过。最新完整检查在审核接入前为 231/231 通过；后续以本节最终记录为准。
- 正在接入微信官方 msgSecCheck v2：帖子、评论和昵称检查在数据库事务之前执行；待审不公开、风险拒绝、服务故障不放行；长正文分段检查。尚未部署或取得本轮真实平台审核证据。
- 下一步：审核/页面竞态回归、资料投影所有权与发布产物检查、版本和契约收口、真实预览及平台验证。旧版本云端仍在运行，不能将本地新增接口视为已上线；完整 R0–R8 验收未完成。

## 校园论坛上线准备审查（2026-09-21，既有记录）

- 用户要求先完成分析与记录，按“没有可用企业或个体工商户”处理；优先让同学尽快实际使用校园论坛，同时控制法律、内容安全及封号风险。普通实现和资料整理由执行者判断，不反复提问，不因暂缺所在地/主体字段停止独立工作。未授权本轮改变核心产品、办理真实主体、发送工单或正式公开发布。
- 已完成[上线准备审查](../../audits/launch-readiness-2026-09-21.md)和[推荐路线/办事材料](../../runbooks/launch-preparation.md)：记录L01—L19的规则/代码证据、影响与关闭条件，梳理数据和Mailgun美国区处理、账号注销/留存、审核举报、防骚扰、未成年人、备案/自评/定级边界及技术发布证据。该清单不是已经完成的整改或法律/平台批准。
- 推荐先核实“现账号主体变更＋实际社区类目”，官方支持个人变更至非个人、目标类型含个体工商户；本账号资格、具体类目及身份/云资源连续性仍需核验。匿名和附属私信独立判断，不一概套交友经营许可，也不通过教育类目隐藏社区。主体不存在不等于必须重建AppID或重做全部代码。
- 手册按“指定成员真实体验验证→正式公开使用”组织，核心闭环为学校邮箱认证→发帖→评论→通知→举报处理；最小保护、明确准入限制和实际首发安全门槛不能跳过，全量架构优化不成为额外行政前置。匿名是否调整首发仍是候选方案，现有功能/账号/认证未被本轮修改。
- 本轮实际 `npm run check` 通过 **221/221**，日志 `dist/launch-audit/local-check.log`，是当时共享工作区G1/G2快照，包含其他任务已完成修复；此后其他任务继续修改业务，不将该结果冒充最终发布版本。生产锁只读在线审计仍为0 critical/4 high/1 moderate，未证明具体可利用路径。新增两文档已纳入 `check:docs` 并通过，未新增治理业务功能、未部署或运行新的云端/真机验收。
- 下一步直接复用技术深查F04/F05修复删除内容通知残留与事务吞错，同时按手册W0准备平台/属地/供应商材料，W2/W3推进审核投诉、协议隐私与数据权利。外部正文已备好但尚未发送；未知信息只在实际办理/定稿必要时集中补齐。原legacy退出条件仍按对应TS/feature与调用者验收，R2—R8未关闭，整改状态只维护本文件。

## 全项目深查与微信官方对照（2026-09-21，技术切片）

- 按用户要求并行审阅客户端、服务端/数据和工程/发布，覆盖 14 个主包页面、1 个聊天分包、6 个函数、18 个集合及工具链。已实际读取微信 Page、setData、分包、Skyline、文本内容安全官方文档，并核对 CloudBase/Node/TypeScript 官方资料；结论及代码位置见[深查报告](../../diagnosis/lynku-upgrade-review-2026-09-21.md)。既有总计划已细化实施顺序，不建立另一套阶段计划。
- 保留工作区已有 miniprogram-api-typings 5.2.3 更新。本轮修复匿名评论未定义变量导致创建失败、实名评论/回复作者跳转使用旧字段、软删除父评论导致有效回复消失三项缺陷。新增 4 条真实入口/组件/轮询行为回归，均先复现失败后修复。匿名字段仍由公共投影生成，未把真实身份补回匿名 DTO。
- 修复前完整 check 为 217/217；修复后完整 `npm run check` 为 **221/221**，依赖图、类型、文档、独立云函数产物等门禁通过，日志为 dist/deep-review-final-check.log。SDK/页面测试使用本地合成夹具；本轮未部署、未修改真实数据、未重新发送邮件、未做新真机验收。因此下方此前六函数部署记录不包含本次修复。
- 新确认的优先缺口：删除内容后通知旧预览、评论序号/幂等读取故障被当不存在、详情/草稿生命周期和身份刷新、搜索查询与游标混用、邮箱启用文案和换绑路径、内容审核适配与发布配置读回。潜在云事务破坏结果与资料投影并发问题明确标待 G3 验证，不冒充已发生事故。
- 官方文档核对修正运行时升级方向：Node 20 已上游 EOL，Nodejs24.11 为 CloudBase 优先验证候选；现有云配置未变。TypeScript 7 的编译器 API 与现有工具不直接兼容，先设计 6.0.3 迁移而非全量 latest。微信审核单次长度/配额与项目 10000 字帖子需明确适配方案。
- 下一步按总计划先修内容删除传播和评论读取故障，再贯通学校认证→发帖→评论→通知的隔离云测试；旧 services/common 仅在对应 feature/TS 用例和全部调用者验收后退出。R2–R8 未因本次分析完成而被标为完成。

## 当前验收结果（2026-09-21 下午，覆盖下方历史状态）

- 开发者工具类型定义提示修复：miniprogram-api-typings 从 5.2.1 更新到 npm 当前 5.2.3，锁文件同步，npm run typecheck:client 通过。本次只更新开发期定义，不改变云端运行配置或上述上线验收边界。

- 唯一维护目录为 LynkU，原 Lucky、旧环境与跨 AppID 的真实身份数据保留。学校邮箱认证已恢复：新环境 users 配置有效服务端凭据与开关，客户端唯一配置也已开启。密钥只保存在云端配置和仓库外 Windows 加密文件。
- 真实云数据库及 Mailgun 不投递测试模式验证通过：发送接受、错误码拒绝、事务绑定、重复确认幂等、同一邮箱排他绑定。两个人工隔离身份通过可信 SDK 适配夹具运行，测试记录已按确切归属清理；这不是两个真实微信账号的真机验收。证据为 dist/demo/mail-validation.json。
- 18 个集合权限已应用并读回确认；真实微信客户端对 17 个私有集合直读均被拒绝。25 项目标业务索引核验无缺失，未删除旧数据或旧非冲突索引。证据为 dist/demo/acl-management.json、schema-inspection.log 和 cloud-initialization 报告。
- 六个业务云函数已重新部署；users/comments 超时设为 20 秒，categories 恢复 3 秒。两个每分钟 timer 已建立并启用（profile-outbox、notification-outbox）。已验证调度入口拒绝客户端伪造；平台定时执行日志及故障重放尚未验收，不能把触发器建立等同实际补偿成功。
- 评论公共 DTO 改为显式字段投影及运行时校验，匿名/屏蔽/删除响应去私有字段；增量读取失败不再吞错后推进游标。服务端入口拒绝畸形事件和非布尔认证值，客户端不回显 SDK 原始错误。
- outbox 回收过期租约、按认领次数隔离迟到完成，并分别查询到期重试与过期认领；通知重复投递保留已读状态。去掉运行中临时建通知集合的兼容逻辑，构建检查 timer 配置与独立上传包的一致性。
- G1/G2 完整 npm run check 通过 **217/217**；G3/G4 部分真实运行时 **26/26** 冒烟通过，包括邮箱开启后的服务端输入验证及临时维护入口移除。预览重新编译成功，总包 464767 B。证据为 dist/release-check.log、dist/demo/smoke.json、preview-info.json。空库公开读取不等于带真实内容的完整社区闭环。
- 临时受限维护代码已由正式 categories 产物替换，真实调用返回 UNKNOWN_ACTION；本地含明文凭据的临时文件及维护 nonce 已删除，剩余维护文件扫描无该凭据。个人 project.private.config.json 已退出 Git 跟踪，保留本机设置。
- 依赖修复消除已发现 critical 项；官方 wx-server-sdk 4.0.2 的剩余传递依赖仍有 **1 moderate / 4 high**。没有通过降级 SDK 隐藏告警。云端仍是 Nodejs16.13；运行时升级不能靠改 manifest 冒充完成。
- **未达预备上线验收**：剩余 JS 业务/客户端 services 的架构迁移、官方 SDK 依赖风险处置、云运行时升级、平台 timer 故障恢复证据、认证用户发帖/评论/草稿/双账号私信全流程、真机生命周期/布局、数据保留与回滚演练仍未完成。旧 AppID 历史内容也未迁入新环境。现有备份与这些未完成项不能通过删除记录来宣称“无遗留”。
- 下一切片优先完成核心业务缺陷回归、隔离环境 Node 24 候选运行时与 SDK 验证、核心业务云事务集成及 timer 重放；运行时方向已按本次官方资料复核修正。通过后再退出对应 legacy services/common。R8 及正式发布仍未完成，未提交审核或发布。

## 历史：LynkU 迁移与演示收敛（2026-09-21 上午）

- 当前唯一维护目录为 `D:/Projects/soupuppy/LynkU`，原 Git 历史保留。Lucky 在 `d2ff90bfd65fc8565c54e3db7cad3ee3efa749b6` 的 423 个源文件迁入；原目录仍干净、未改写。LynkU 模板归档于忽略的 `dist/private-backups/lynku-template-20260921`，旧身份/数据备份 11545 个文件已逐一核对 SHA256，存于新目录的 `dist/private-backups/identity-20260921`，不提交 Git。
- 用户确认原生微信 + CloudBase，不切换 Taro/React。延续 apps/packages/tooling 边界，内部 @lynku 包名保持稳定；根工程与界面品牌为 LynkU。默认分类定义进入 content 的严格 TS 公共出口，管理种子与初始化复用。
- `config/project.json` 成为 AppID、环境、基础库、展示名及功能开关的唯一来源；`npm run configure` 同步三个配置文件，门禁拒绝漂移与秘密字段。新增 setup/dev/deploy:wechat/demo:preview/demo:verify/cloud:inspect/cloud:init 可执行入口。初始化和部署有目标校验；微信 CLI 退出码为 0 但输出错误时仍报失败。
- 新 AppID `wxba2bcb0c71a5f33d` 的微信环境列表确认环境为 `cloud1-d7gifgdpb8ad0ab8e`，配置已切换。旧环境及旧认证/内容未改动，不按新旧 OPENID 自动合并。新环境通过微信开发者工具自己的授权操作；收尾时 tcb 环境列表为空，旧 users 配置导出也返回 env not found，未取得邮件密钥或管理配置权限。
- 六个业务云函数已部署到新环境并报告成功；初建阶段 Creating 状态导致的上传失败已在状态转为可更新后重试成功。18 个集合、23 项目标业务索引已在实际新环境核对；users 默认非唯一 `_openid_1` 被受控替换为唯一索引，未删用户。5 个既有默认分类通过受控种子插入，未伪造认证账号或历史内容。
- G1/G2：迁入后完整 `npm run check` 通过 **207/207**（包含新增配置行为测试）。G3/G4 部分：真实微信运行时 7 项冒烟通过，覆盖新 AppID、自动账号恢复、分类/帖子读取、未认证私信拒绝、首页空态和资料/设置导航。`dist/demo/smoke.json` 为无个人资料的机器记录。预览实际编译成功，总包 459030 B，主包 438989 B，聊天分包 20041 B；二维码与报告在 `dist/demo`。这些不是发帖/私信全流程或真机验收。
- 明确阻塞：新集合默认权限实测允许本人客户端读 users，未达到 admin-only 要求。已保存无用户内容的 dist/demo/direct-db-check.json；官方微信技能 CLI 无权限配置操作，当前 tcb 凭据无可见环境。未部署临时管理函数或提升云角色。云端 users 默认超时实测 3 秒，与 manifest 的 20 秒尚未对齐。邮件凭据与完整验证码/绑定尚未验证，邮箱入口仍关闭。不能把基本浏览演示称为正式上线或完整社区闭环。
- 新增 demo:verify 的 17 个私有集合客户端直读拒绝检查，权限错误以外的失败不作为通过。该扩展未在用户停止窗口控制后执行，旧 smoke.json 的 7 项成功不包含此门禁；现有 users 直读失败证据使整体演示验收不通过。微信部署命令也明确提示需要独立核验运行时配置。
- `cloud:init` 会把平台确认任务写入 `dist/cloud-initialization/journal.json`；续跑先查询旧任务，每个操作最多提交一次，不轮询刷写、不覆盖既有索引冲突。集合/索引准备不代表权限已经应用。后续索引 schema 变更须单独迁移，不把初始化 journal 当通用迁移框架。
- 开发者工具曾出现 MCP/自动化超时，重启后恢复；窗口截图接口仍报 project window not found。用户按 Escape 后桌面控制已停止，不继续 UI 自动化。原有设备布局、生命周期全矩阵及 G3/G4 剩余项保留。

## 项目结构切换完成（2026-09-21）

本节更新下方交接点中“结构任务仍未完成、类型检查失败”的临时状态；保留下方真实云端迁移记录，不重新执行账号清理。

- 官方资料调研与决定见 [ADR 001](../../architecture/decisions/001-project-structure.md)。运行时源码迁至 `apps/miniprogram`、`apps/cloudfunctions`，构建/检查迁至 `tooling`；`scripts` 保留真实部署与数据迁移命令。微信开发者工具仍打开仓库根目录，页面 URL/函数名不变，原 app.json 内容随迁移保留。
- 编辑器保存、发布、幂等、版本与恢复进入 `features/editor`，页面通过 composition 装配业务与微信存储能力。16 个 TS 行为用例覆盖原 9 组页面行为，并修复 hide/show 后草稿版本滞留、发布按钮锁定及完成态重新显示无法退出；旧实现细节断言已替换，账号隔离与草稿清理测试保留（A01/A10/A11/A14/A15）。
- 消息及通知读取的持久化适配进入严格 TS `packages/adapters`，messages 入口不再直接查询集合；6 个适配行为测试覆盖事务、并发幂等、游标/账号范围与畸形响应。既有匿名、权限、消息行为随完整检查回归（A02/A03/A04/A05/A07）。
- esbuild 从当前源码解析实际依赖，每函数生成独立 index/config/package/lock；六份源码 utils 副本已退出。`cloudbaserc.json` 是函数清单与运行时来源，部署脚本只上传构建产物。公共运行时锁保留 wx-server-sdk 4.0.2 及传递版本，不依赖旧包 dist 或本地 workspace 链接。
- dependency-cruiser 与平台全局检查接入门禁，25 个真实工程夹具证明循环、私有深层导入、SDK 耦合及聊天分包越界会失败。移除契约类型循环、重复且未使用的 SDK 类型目录和过时全局 app 类型，identity 增加公共出口。
- **实际验证：工作区 `npm run check` 通过，205/205 测试通过；在仓库外仅复制源码与锁文件、无 node_modules/dist 的临时副本执行 `npm ci --ignore-scripts` 后再次 `npm run check`，同样 205/205 通过。** 六个函数均通过隔离加载、配置/运行时/摘要/锁文件核验；消息 bundle 实际执行可信入口到适配器的合成会话目录流程。三个部署 shell 脚本语法检查通过，CloudBase CLI 3.8.3 的 deploy 帮助已核对。上述为 G1/G2，不是实际部署/云事务证据。
- 本次结构工作未调用部署或数据迁移命令；用户原有页面配置和 `dist/private-backups` 保留。工程仍使用 Node 24.21.0、TS 5.9.3；云目标仍为 Nodejs16.13。SDK 的现存传递依赖审计为 1 moderate、5 high，已记录，未以降级 SDK 或强制升级绕过。
- 下一步：按原阶段继续收敛其余 services、评论/分类 JS 业务及资料投影写入所有权；验证微信开发者工具/真机布局、分包与生命周期，核对真实云端 SDK、索引、历史消息迁移及邮箱开关。旧源码路径和 utils 双份维护已无保留需要；尚存 legacy services/common 是待迁业务，不称为已完成全栈 TS。R8、邮箱恢复及 v1.0 仍未完成。

## 迁往新项目的交接点（2026-09-21）

用户要求快速收敛并提交，当前任务停止扩展实现。以下为最新实际状态，优先于下方历史切片记录：

- CloudBase 环境仍为 cloud1-d4g94y77f0a618eb5。users/posts/comments/messages/categories/drafts 六个函数已部署成功；废弃 login 已删除，旧 common 云函数尚未清理。运行时仍为原 Nodejs16.13，尚未升级。
- 身份迁移已实际完成并逐字段核对：48 条账号归并为 10 个微信身份，4 个已认证账号及原 ID/微信关联保留，38 条重复记录完整保存于本地备份后删除；主账号补默认字段、回填 4 条 email_claims。旧非唯一 `_openid_1` 索引已删除，`unique_openid` 唯一索引已建立。私人迁移 journal 的 completed=true。
- schema manifest 的 18 个集合已建立/确认存在并应用客户端权限：categories 为 ADMINWRITE，其余 ADMINONLY。除 users 唯一索引外，其他目标索引尚未整体应用/验收。
- 25 条旧帖子补齐 revision=1、缺失 anonymous=false 和分类快照；真实云端 posts.list 调用成功（23 条公开帖子，抽取 1 条），匿名响应没有真实作者字段。尚未取得用户重新编译后的设备验证。
- 邮箱发送/确认应用代码已部署，但没有配置本次 Mailgun 密钥或启用邮箱开关，邮箱恢复仍未完成；此前测试邮件已由用户确认收到。不得把发送测试或集合已建称为完整认证上线。
- 85 条旧私信尚未迁入会话目录/序号模型，20 条评论及 16 条通知仍需核对新 schema；不能把新 messages 部署成功称为历史消息迁移完成。outbox 调度、全量索引、运行时升级、设备验收及其余 R2–R8 工作仍在原目标范围。
- 本次执行 npm run check，在客户端类型检查阶段失败：另一个正在运行的任务“对比 LynkU 小程序结构差异”正在同一目录建立 editor controller、composition、adapters 与 tooling；当时缺少 controller，并有 composition 参数隐式 any。该任务的未完成改动不纳入本任务的收尾提交，也不回滚。迁移到新项目之前须由该任务完成并提交其工作。

迁移项目时须单独保留被 Git 忽略的 `dist/private-backups/identity-20260921/`：包含 users 原始备份、完整重复记录归档、迁移 journal、旧 users/login 云函数及内容迁移前快照。这些私人文件不会随 Git 提交转移，不应提交到仓库。`project.private.config.json` 也不随 Git 转移；新项目应自行选择开发者工具配置。新项目若使用不同 AppID/CloudBase 环境，需重新核对微信身份与数据归属，不能假定旧 OPENID 自动对应新 AppID。密钥只配置到服务端。

恢复入口：先读取本节与实际工作区，再完成另一个任务的结构切换，随后优先处理私信历史迁移和邮箱配置/核心云端验证。不要重新执行已经完成的账号删除；`scripts/apply-identity-migration.js` 是本次已执行迁移的工具，旧非唯一索引移除是本次单独执行的前置步骤。完整 v1.0 尚未完成。

## 已有基线证据

- 基线 HEAD：`7c78ef3976811caf776582e9fc201096058f2a25`。工作区存在用户/先前任务未提交改动，尤其 `miniprogram/app.json`，不等于此 HEAD 的原始状态。
- R0 动作/权限盘点与已确认差异见 [baseline.md](baseline.md)。
- 本轮实际运行：在 `npm ci --ignore-scripts` 的干净依赖安装后，`npm run check` 通过，58 项测试通过，依赖审计为 0 漏洞；检查包含客户端/共享包/服务端 TypeScript、架构和文档检查、客户端/部署环境标识一致性、13 个云函数源文件语法检查与独立产物检查。
- 本次检查环境：Windows PowerShell、Node v24.21.0、npm 11.19.0、仓库 TS 5.9.3。它们是观测值，不代表已核验的云端支持矩阵。
- 代码基线的现有修改覆盖 users/posts/comments、会话/登录/认证/角标/轮询、相关页面和开发说明；`miniprogram/services/verification.ts`、`tests/guest-session.test.js` 也是需要保留的未跟踪基线文件。
- R1 已建立 npm workspaces、严格共享 contracts/server 项目、架构/文档检查和可重复共享包构建；6 个函数能从唯一 manifest 生成并通过独立产物检查，部署改为使用这些产物。未部署、迁移数据库或恢复邮件。
- 已完成只读 CloudBase 基线审计，详见 [cloudbase-live-baseline-2026-09-21.md](../../audits/cloudbase-live-baseline-2026-09-21.md)：环境可访问，但函数、集合和索引仍是旧状态。微信开发者工具/真机验证、隔离云端事务与权限验证、受控 schema 迁移、部署/回滚演练尚未执行。

以上不替代 R0 的完整接口盘点、行为差异复现和工作区快照保护。

## 阶段状态

状态使用：未开始 / 进行中 / 本地完成 / 待外部验证 / 完成。只有达到阶段退出条件才推进；待验证具体记录在下表证据中。

| 阶段 | 状态 | 已完成证据 | 下一步 |
|---|---|---|---|
| R0 基线 | 本地完成 | `baseline.md`、32 项检查通过 | R1 完成独立云函数产物试验 |
| R1 工程底座 | 本地完成 | `build:packages`、`build:cloud`、独立产物检查、34 项检查通过 | R7 再用干净安装和实际微信加载复验 |
| R2 契约授权 | 进行中 | 所有 6 个函数接入统一 action policy；私信/草稿私有读认证；删除先授权后幂等；鉴权查询故障返回 `AUTH_UNAVAILABLE`；创建类限流存储故障拒绝写入；基础 contracts 包；运行时错误码与 contracts 对齐 | 将动作 schema/响应 DTO 接入入口，替换 JS 兼容层 |
| R3 消息 | 进行中 | 会话同步已从时间戳切换为会话作用域的序号游标；发送事务写入会话计数器与双方目录摘要；私信请求键与负载绑定；通知读取不再触发历史扫描写入；56 项本地检查通过 | 为目录与已读位置补充迁移、索引和真实 CloudBase 事务验证 |
| R4 内容通知 | 进行中 | 帖子、评论、草稿和私信均支持稳定请求键、重复结果与负载冲突检测；帖子和草稿更新以 revision 拒绝陈旧写入；评论重复请求不重复计数或通知；评论变更已在创建/删除事务中写入按帖序号的 `comment_changes`，客户端以游标增量合并；评论通知在同一事务写入 `notification_outbox`，消费者以共享租约认领、指数退避和幂等通知投递；行为测试覆盖首次投递与幂等重试；58 项本地检查通过 | 为 outbox 接入受认证的调度消费者和告警，并在真实 CloudBase 验证计数器、变更记录和 outbox 事务/索引 |
| R5 身份草稿 | 进行中 | 公开资料 DTO 已移除邮箱、验证状态和角色；创建/更新帖子与创建评论复用入口已认证 Principal 生成公开作者快照，避免同请求重复读取 users；本地草稿回退按登录账号隔离；资料更新改为版本化 `profile_outbox` 投影，消费者读取当前资料防止旧事件回写；两类 outbox 共用租约/退避工具并有行为测试；草稿创建/删除和 50 条计数器在同一事务，更新使用 revision 冲突保护并在客户端保留冲突内容 | 收敛服务端业务 TS，并接入 outbox 调度/告警 |
| R6 前端结构 | 进行中 | 唯一遗留的 `email-verify` 页面脚本已迁为 TypeScript，改用统一认证、会话和 users 服务；客户端严格类型检查通过 | 继续收敛编辑器、页面状态与最终目录，并在开发者工具验证页面加载 |
| R7 工程治理 | 进行中 | 根 `check` 已验证 machine-readable CloudBase schema manifest，覆盖 16 个集合、索引、客户端权限和函数引用完整性；每个云函数入口与统一 action 策略双向核对；公开错误码与共享 contracts 联合校验；客户端、开发者项目与部署清单的环境/AppID 标识一致性已纳入门禁；GitHub Actions 从干净依赖安装执行同一质量门禁且不持有部署凭据 | 真实控制台对比、清理与完整本地验收 |
| R8 平台验收 | 进行中 | LynkU 基础浏览 7 项真实冒烟、实际预览及集合/索引核对 | 先修复数据库客户端权限、users 超时和邮箱配置，再完成事务、业务闭环与真机验收 |

## 当前下一步

用户明确允许当前原型破坏性重构，并要求加快推进：采用配套客户端/服务端直接切换，不再维护旧客户端兼容矩阵；保留指定账号和已有认证，验证集中于权限、身份保留、匿名隔离与核心流程。已同步 AGENTS、产品契约、接口迁移规范及执行计划。已下载备份旧 users/login 函数，实际删除云端废弃 login，CLI 返回 Function deleted。users 旧入口尚未替换，身份数据迁移尚未应用；下一步直接完成 users 切换、重复账号清理及邮箱集合/配置。新增快照比较支持导出和查询的两种 BSON 日期表示，独立查询与备份比较无变化；该结果只对应采样时刻，不作为后续写入锁。

云端身份准备取得实际证据：只读 users count=48，导出备份 48 条且生成 SHA-256；10 个微信身份，4 个已认证账号，2 组重复身份共 38 条冗余记录，hvydw1 记录存在。新增并实际运行只读迁移 planner，保留唯一已认证主记录与原 ID，规划完整归档、补字段和邮箱归属；冲突情况拒绝猜测，原数据/详细计划只在忽略目录。`npm run check` 通过 166 项测试，含迁移保留性与冲突拒绝验证。详见 [云端身份审计](../../audits/identity-live-audit-2026-09-21.md)。未应用迁移、未删真实记录、未部署/开启认证；下一步核对旧写入入口与新变更，实施可回滚归档/迁移并完成云端验证，继续恢复邮箱。

邮箱恢复发送链路：users.sendEmailCode 接入 TS sendSchoolVerification，原子预留账号冷却/每小时配额和验证码挑战；实际发送置于事务外，generation 校验保护完成状态及 email_pending。每账号只保留一个挑战，新邮箱请求使旧挑战失效；确认路径对应调整。新增 email_send_limits 私有集合 manifest，移除请求内建集合和宽泛“错误等于不存在”回退。`npm run check` 通过 164 项测试、18 集合 schema 和独立构建；验证并发不同收件地址仍仅一次发送、配额上限、预留回滚不发送、供应商失败保留冷却、旧回调不覆盖新挑战，以及发送后确认绑定的联通行为。未部署/迁移/启用。下一步核对真实 CloudBase 集合/账号唯一性、备份与归属回填、隔离云端事务、设置服务器密钥并验证客户端流程后开启；仍保留明确恢复授权。

邮箱恢复：确认路径已迁入严格 TS confirmSchoolEmail 并接入 users.verifyEmailCode，新增 email_claims 私有集合 manifest。归属竞争、账号认证和验证码消费同事务；错误次数在事务中提交，5 次并发上限；sent/过期/归属校验与成功重复确认均有行为测试。`npm run check` 通过 160 项测试、17 集合 schema 和独立产物验证。未在云端建集合/部署或开启认证。下一步发送请求的原子冷却与挑战版本保护、历史认证归属回填/唯一性核对、云端密钥配置和端到端验证，然后按已授权要求开启；不能因确认模块通过测试就声称邮箱功能恢复完成。

邮箱恢复第一步已接入代码：验证码模板迁入实际使用的 TS verificationMail，固定已验证的 mail.oikoss.cc 与 LynkU <noreply@oikoss.cc>，学校域名/6 位码验证，移除 lucky.asro.cc 旧发送配置。HTTP 层限制响应 64KB、处理响应中断，只有有效 Mailgun 消息 ID 才确认提交，不再把任意 2xx/原始正文当成功或记录供应商原始错误正文。`npm run check` 通过 157 项测试；现有源码模板位置断言改为检查实际渲染结果。密钥未写仓库，未再次发送真实邮件。认证开关仍未启用：下一步必须修复邮箱占用事务外检查、验证码发送冷却竞争、失败与重试状态，再完成端到端验证/云端配置后开启；已获得用户明确恢复授权，不需再次询问。

R5/A03 作者投影防回退：实际 posts/comments/notifications 更新谓词绑定所有者、非匿名和较旧/未初始化的快照版本；版本验证迁入 TS profileSnapshot。新发帖/编辑帖及公共作者快照携带源 profile_version，缺失/非法源版本拒绝生成快照；测试账号夹具补齐实际 Principal，删除依赖缺失 Principal 的回退预期。行为测试人为延迟版本 1 写入，先完成版本 2，验证旧任务不能覆盖新昵称且匿名/其他作者不受影响。`npm run check` 通过 155 项测试，独立产物通过。既有账号版本迁移、实际条件查询索引、延迟创建内容的快照刷新、消费者租约/调度与 R8 仍未完成。

用户已确认测试邮件实际收到，并明确授权恢复学校邮箱认证。下一优先项为接入 mail.oikoss.cc / LynkU <noreply@oikoss.cc>，验证完整验证码与账号绑定安全后开启；此授权替代之前的长期暂停要求。当前开关尚未切换，密钥不写仓库。

R2/R5/A01 资料更新事务应用：移除 JS 更新/入队业务，迁入 updateAccountProfile 严格 TS；昵称/头像输入验证、事务内所有权复核、账号 DTO 校验、安全整数 profile_version，以及账号修改/事件写入原子性均接入实际入口。无变化重复提交不增加版本/事件，新账号初始化 profile_version=0；旧账号缺失字段要迁移，不再以 Number 强制转换掩盖脏数据。提交后投递异常被隔离，返回已提交账号结果并保留 outbox 重试。`npm run check` 通过 154 项测试，新增入队失败回滚、并发相同修改仅一次、脏版本/输入/归属/查询失败验证。独立产物通过。剩余重点：投影消费者版本条件写防旧结果覆盖、可靠调度/告警、真实事务和旧记录迁移；公开分页及 R8 仍未完成。

外部邮件验证补充：用户授权向指定测试邮箱发送一封邮件，使用 mail.oikoss.cc 与 LynkU <noreply@oikoss.cc>，Mailgun 返回 HTTP 200 / Queued（2026-09-21）。仅证明服务商接受请求，未验证最终投递或邮箱认证端到端；密钥未保存到仓库，现有项目邮件配置与认证开关尚未据此切换。用户提到未来品牌可能为 LynkU/Lynk，最终名称未定，不进行全项目改名。

R2/R5/A01 微信账号初始化 TS 应用：ensureAccount 明确已有账号只读、新账号默认未认证、读取失败不创建；新账号固定 ID 且事务先读后写，多次/并发调用复用结果。users.ensure 移除资料修改和缺失字段读时修补，拒绝 nickname/avatar_url；既有账号原 ID/认证/角色不动，全部 users 账号查询改用保留异常的方法，避免失败伪装成不存在。新增应用与云函数事务适配测试，`npm run check` 通过 152 项测试及独立产物验证。schema 文档标明新账号 ID 策略及保留既有 ID。未写入真实账号、未部署；上线前仍需核对唯一索引、修复旧记录 schema、停用旧随机 ID 创建函数并真实验证事务。继续用户更新/投影应用 TS 收敛及公开内容分页，R8 未完成。

R2/R5/A03 公开资料纵向边界：users.getProfile 迁入 TS 应用和 PublicProfile 契约，严格验证 ID、归属、字段和注册时间；实际适配器改用 findUserByOpenid 保留查询异常，替换旧吞错查询方法。客户端校验响应 ID/会话代际，作者页不再以默认账号掩盖资料不存在；移除额外且吞错的 getPostCount，统计复用公开实名列表 total，失败显示不可用。补齐页面隐藏/销毁代际保护。`npm run check` 通过 149 项测试，包含真实云函数适配层数据库故障返回 QUERY_ERROR 的验证、字段脱敏/非法 ID/不存在区别及页面失败状态；独立产物通过。未部署、未改真实用户数据。其他身份写入仍有旧查询和 JS 业务，公开列表 offset、服务端用户初始化/更新应用 TS 收敛与 R8 仍待继续。

R2/R5/A01 私人账号 DTO 和缓存边界：共享严格 SelfProfile 供自动微信身份、资料修改、认证结果与缓存恢复使用；服务端 users 的三个账号返回动作已接入 TS 归属校验/日期规范化/字段映射，移除原始用户记录透传。客户端账号变更结果要求同账号及同会话代际；缓存损坏时只读取一次，即使删除失败也不再次恢复残留内容，后续微信 ensure 可正常建立会话。新增行为验证覆盖字符串认证拒绝、日期错误、内部字段剔除、账号不匹配和损坏磁盘；原有投影测试数据补全必需账号字段。完整本地检查通过 145 项测试及 users 独立云函数产物构建。未部署、未改远端账号；用户数据库写入应用、公开资料 DTO、身份查询故障语义和真实平台启动仍待继续验证，R8 未完成。

R2/R5/A01 微信自动身份已本地实现：启动调用 users.ensure，登录页自动加入同一账号代际的进行中请求；删除持久游客选择入口，未认证微信账号显示游客。共享身份服务从 unknown 校验用户字段，verified 必须为 boolean、role 必须为既定角色，成功仅写入一次会话；清理/换账号后旧响应拒绝提交。失败可公开浏览和重试；隐藏登录页不再迟到导航，个人页在可见期间订阅身份变化。设置改为说明准确的“清理本地会话”，返回首页且保留远端账号/认证，下次启动恢复身份。服务端行为测试验证既有账号 ensure 无写入且保留认证、角色与邮箱，不信任请求伪造字段；hvydw1 仅为保留账号要求与测试名称，不是白名单。`npm run check` 通过 141 项测试，独立产物检查通过。未修改/查询实际账号数据，真实微信启动和云端账号对应关系待 G3/G4；身份数据库模型/共享 DTO 的全面 TS 收敛、缓存 schema 校验以及公开列表分页仍待继续。

优先方向变更（用户 2026-09-21 明确要求）：微信自动登录作为基础身份，未认证微信账号就是产品游客，邮箱只做认证升级；保留已有微信测试账号及 hvydw1 既有账号状态。已更新 AGENTS 与产品契约，旧“浏览永不创建账号/必须主动登录/持久游客选择”不再是目标。核对 users.ensure 当前会按 OPENID 找回已有账号并保留已存在的 verified/role，本轮未改云端数据。**客户端尚未切换**：app 启动不 ensure、登录页仍有独立游客选择，相关旧测试和文案需随实现替换。下一步先完成自动身份请求去重、服务端结果验证、退出/失败/迟到响应隔离和已有认证账号回归，再继续公开帖子分页；不得把本次契约修订称为身份流程已经上线。

R2/R4/A03 私人帖子稳定分页：`posts.listMine` 已独立迁入严格 TS 应用与共享请求/响应契约，按创建时间和 ID 降序、账号绑定游标、limit+1 判断后续页，拒绝 offset。我的帖子页新增滚动加载、同页失败重试、刷新状态和账号/页面响应隔离，解除首批 50 条访问限制。`npm run check` 通过 138 项测试，行为证据包括同时间戳 75 条完整遍历、期间新增/删除不造成偏移跳项、跨账号游标在查询前拒绝、失败不推进游标以及隐藏后不写页面。独立云函数构建通过。私人列表索引已补 `_id`，尚未云端应用；真实滚动/索引查询待 G3/G4。该切片不提供事务快照，公开列表与搜索的旧 offset、其他内容模块 TS 迁移及 R8 仍未完成；下一步继续公开分页和剩余业务迁移。

R2/A03 公开作者与本人内容分离：新增 verified 的 posts.listMine，可信调用者绑定本人，返回已发布/待审内容；公开作者筛选始终排除匿名内容，不再因调用者恰好是作者而扩权。我的帖子页切换至私人接口并清除跨账号迟到状态；public_only 不可读取私人列表。核对发现此前搜索函数仍直接返回泛型 cloud 结果，现已实际接入共享页面解析，修正此前“搜索已校验”的证据缺口。`npm run check` 通过 135 项测试；新增私人查询索引尚未云端应用。稳定分页及我的帖子超过 50 条加载仍待实现，后续继续迁移。

R4 帖子详情用例迁移：请求布尔值/ID 校验、公开/作者/管理员可见性、投影和浏览计数流程迁入严格 TS；编辑读取固定不计浏览，游客拒绝待审内容且不查管理员，身份查询故障不被吞成普通非管理员。DTO 验证和安全整数上限检查先于浏览写入；详情使用保存时的分类快照，移除该路径的额外分类回退读取。`npm run check` 通过 132 项测试，覆盖权限矩阵、编辑副作用、非法参数、计数溢出及身份查询故障（G1/G2）。列表/搜索的授权边界和稳定分页、评论、分类管理、身份与 G3/G4 尚未完成。

R2/R4 帖子读取 DTO：新增严格共享 PostView/页面解析与服务端记录投影，客户端详情、列表、搜索均校验响应；时间统一 ISO 字符串，计数/版本/匿名状态/分类关系显式验证，匿名嵌套身份与未知字段不返回。公开页面响应拒绝未发布条目及重复 ID；详情在浏览计数写入前验证记录，数据库异常不再按泛化 -1 伪装不存在。`npm run check` 通过 130 项测试，新增隐私投影、日期与畸形状态/页面验证（G1/G2）。读取授权/分页应用层与个人内容查询边界仍需收口，现有 offset 分页尚未迁移；评论、身份及 G3/G4 待完成。

R4 帖子删除/审核迁移：严格 TS 状态用例从可信 Principal 接收管理员能力，事务内复验所有权/当前状态，分类读取在写入之前；状态与计数一起提交。重复删除/相同审核不重复计数或增版本，实际变化递增 revision，阻止旧编辑覆盖；重新发布要求分类仍有效，已删除帖子不能审核恢复。`npm run check` 通过 128 项测试，新增回滚、并发幂等、管理员/作者/陌生人及删除后权限场景（G1/G2）。帖子写入主流程已 TS 化，读取/分页、评论、分类管理、身份及 G3/G4 尚未完成。

R4 帖子更新迁移：更新请求强制 expected_revision 与 boolean anonymous；严格 TS 用例在事务内复验作者/版本，并先读完相关分类后一起更新帖子与分类计数。成功更新记录绑定原版本和负载的指纹；在该更新仍是最新版本时，重试直接返回确认，不重复计数且不受新修改限流影响；不同负载或更新后的旧请求冲突。帖子公开读取改为显式字段映射，内部请求 ID/指纹不返回。`npm run check` 通过 126 项测试，覆盖分类迁移失败回滚、并发重试、错版本/作者/分类及响应字段（G1/G2）。删除/审核、评论、身份、完整读取 DTO 及 G3/G4 仍待推进。

R4 帖子创建迁移：严格 TS 请求/用例接入 CloudBase 适配器，强制请求 ID 和 boolean anonymous；删除无请求键创建、读取故障当缺记录和客户端分类快照分支。事务先读帖子/分类再写帖子与分类计数，匿名快照由服务端生成，返回最小发布确认。成功请求重试先确认指纹再走新建限流。`npm run check` 通过 124 项测试，新增计数写失败回滚、并发去重、匿名字段、快照注入拒绝、待审不计数和读故障拒绝（G1/G2）。帖子更新/删除/审核、评论、身份仍待迁移；真实 CloudBase 事务及旧指纹/版本/计数迁移待执行。

A10 未确认草稿与发布衔接：发布前先用冻结的创建请求确认草稿 ID，失败不发布、不丢弃恢复记录；成功后发布当前正文，并仅对创建版本 1 排队清理，避免以重试返回的新版本误删其他编辑。`npm run check` 通过 122 项测试，覆盖确认→发布顺序、旧草稿/新正文分离、版本 1/2 清理边界及确认失败（G1/G2）。接下来推进内容服务端严格 TS 迁移，并处理更新幂等、编辑器加载恢复及云端/设备验证；完整目标未完成。

A10 草稿清理补偿：发布成功后的删除改为先保存账号隔离任务，再带 expected_revision 条件删除；较新版本冲突不删除、不扣计数。复用前台维护轮询，每轮最多 5 条，失败指数退避上限 60 秒；后台/账号切换停止后续任务，持久化失败保留云草稿并提示手动清理。修复后台账号变更重新启动轮询。`npm run check` 通过 120 项测试，覆盖版本保护、重启恢复、暂停重试、账号隔离及失败退避（G1/G2）。尚需处理发布时未确认的新建草稿、加载恢复与更新原帖的响应丢失；云端数据/SDK/设备验收未完成。

R2/A10 发布确认契约：客户端创建/更新现在从 unknown 响应解析最小确认 DTO，校验帖子 ID、正整数版本、发布/待审状态与 flagged 一致性；创建还要求 created/duplicate，更新绑定原帖 ID，账号代际变化拒绝结果。无效确认保留发布锁与恢复记录，不删除草稿、不跳转。`npm run check` 通过 116 项测试，覆盖畸形确认、错误帖子、账号切换和编辑器清理前置条件（G1/G2）。服务端 posts 仍需严格 TS 迁移及返回字段收口；更新幂等、草稿清理补偿、加载恢复和 G3/G4 未完成。

A07/A10 发布请求恢复：新帖发布固定请求 ID、正文、分类和匿名选择；结果不明时锁定编辑并显示重试确认入口，重试复用原始负载。恢复记录升级为严格 v2，保留发布请求、编辑模式、原帖 ID/版本，恢复编辑不会被当作新建；确认无副作用的参数/权限/限流拒绝会解除发布锁。`npm run check` 通过 114 项测试，新增响应丢失后的请求恢复与负载一致性、原帖编辑恢复验证（G1/G2）。发布返回 DTO 仍需运行时验证，更新原帖的响应丢失处理、草稿清理补偿和设备恢复体验尚未完成；下一步优先收口这些缺口。

A11 编辑器迟到请求：帖子/草稿读取同时检查页面代际、账号代际与输入版本；分类读取与发布流程检查页面有效性。等待自动保存后再次确认页面有效才发布，重复提交被拒绝，失效发布结果不提示、不清草稿、不跳转；隐藏/卸载/切换账号取消延迟导航。`npm run check` 通过 113 项测试，覆盖两类读取在输入/隐藏/换账号后的响应，以及发布等待保存或等待服务器时隐藏（G1/G2）。下一步仍需处理发布请求固定负载及持久恢复、加载中编辑/再次显示页面的恢复、成功发布后的草稿清理补偿；不能视为完整 A10/A11 或设备验收完成。

A10 编辑器创建重试：首次草稿创建固定请求 ID 与原始负载，确认后再以草稿 ID/版本保存后续输入；按账号保存带版本且经校验的恢复记录，包含未确认创建请求，删除全局 draft_emergency 读取。自动保存丢弃卸载/隐藏/会话变化后的结果，存储失败不再报告本地保存成功。`npm run check` 通过 111 项测试，新增响应丢失后原内容重试、后续输入保存、账号恢复隔离、存储失败和卸载竞态（G1/G2）。下一步继续编辑器发布请求的负载绑定、迟到读取/发布生命周期、编辑模式恢复与草稿清理；设备持久化、弱网和云端事务验收尚未执行。

R5 草稿业务迁移：创建/更新/列表/删除已迁入严格 TS 用例，CloudBase 仅提供鉴权、限流、文档事务和时间映射。创建强制 request_id，更新强制 expected_revision；公开 DTO 不返回 _openid、请求指纹等内部字段。删除宽泛查询异常吞掉、事务内历史扫描及无请求键创建分支；客户端删除云函数缺失时的本地兼容回退，并验证响应和账号代际。`npm run check` 通过 108 项测试，包含创建/删除计数一起回滚、重复创建、版本竞争、权限拒绝和客户端账号变更（A02/A07/A10/A15，G1/G2）。schema 增加草稿排序 ID；上线前必须回填旧 revision/计数器并验证 SDK 事务和索引，未执行云端写入。下一步核查编辑器保存中继续输入、首次创建响应丢失后重试的请求键/内容绑定，再继续身份、内容和 outbox 迁移。

R4 通知读取迁移：列表、计数、显式已读用例迁入严格 TS，与客户端共用公开 DTO 运行时校验。分页使用账号/筛选作用域的 `(created_at, _id)` 游标，删除旧 before 与省略 ID 的全部已读行为；匿名响应显式剥离身份。两个页面追加分页、逐页已读并拒绝退出后的迟到响应。`npm run check` 通过 106 项测试，新增 55 条同时间分页、跨账号/筛选游标拒绝、匿名嵌套字段过滤、云函数可信收件人条件和双页面生命周期行为证据（A03/A05/A11，G1/G2）。schema 新索引尚未应用云端。下一步继续内容/身份用例 TS 迁移及 outbox 调度与告警；真实云事务、数据迁移和设备验收仍未完成。

A04 来源隔离补充：用户明确匿名私信按发现作者的具体帖子/评论建立通道，不能按真实对端合并。产品契约已细化同来源复用、不同来源/访客隔离；`tests/conversation-target.test.js` 新增同作者不同帖子、帖子与评论同 ID、不同访客、重复进入及跨来源线程替换的行为验证。该批 `npm run check` 通过 102 项测试（G1/G2）；现有服务端目标解析已具备该规则，本批不改变路由算法。云端与设备的完整 A04 验收尚未完成。

R3 匿名目标授权：已迁入共享契约和严格 TS 用例，删除旧参数别名、消息扫描和参与者推算；已有线程按调用者目录授权，来源删除或切换实名不影响已建立会话。首次历史响应让页面绑定确定线程，新会话仍要求公开匿名来源（评论父帖亦需公开）。101 项本地测试通过，覆盖双方访问、来源删除、伪造/跨用户线程、混合参数和云函数到页面的线程传递。消息主体用例已迁入 TS，但 CloudBase 适配器仍为 JS；通知业务迁移与云端 schema/数据迁移仍未完成。

R3 发送事务：核心发送流程迁入严格 TS 用例，以同一事务写消息、计数器和双方目录；显式校验幂等指纹，删除旧指纹推算和宽泛吞错新建分支。已检查锁定 SDK 发布包，消息数据库设置 throwOnNotFound: false，将缺记录与查询故障分开。新增收件账号存在性检查，发送响应接入共享公开 DTO。97 项本地测试通过，覆盖四记录回滚、并发幂等、损坏计数器和提交确认丢失；匿名目标解析、通知业务仍在 JS，真实云事务与迁移验收未完成。

R3 已读重试：新增账号分区持久队列，只保存目标和消息 ID；复用前台角标轮询，每轮最多 5×20 条、失败退避上限 60 秒，成功后清理，后台/换账号停止后续批次。94 项本地测试通过，覆盖重启恢复、账号隔离、去重、暂停恢复、存储失败及损坏记录拒绝。存储不可写时仅保证进程内保留，设备持久化与真实云事务仍待验收。下一步迁移发送事务并清理旧兼容/异常吞掉分支。

R3 回执同步：新增 verified 的 getReadReceipts，经可信发送者/会话/ID 过滤并返回明确 readIds；聊天轮询按 20 条轮换查询已发送未读消息，停止后忽略迟到回执。消息合并保证已读状态不被旧快照覆盖。91 项本地测试通过，覆盖回执权限、字段范围、轮换上限与停止竞态。schema 新增回执查询索引，尚未云端应用；收件侧已读失败自动重试、发送迁移和真实云事务验证继续推进。

R3 已读一致性：已读动作迁入严格 TS 用例，强制显式 1–20 个消息 ID；消息事实、目录未读数及最后消息状态在同一事务内更新，重复请求不重复扣减，计数损坏时失败回滚，读取不改变会话活动时间。客户端对 250 条等大批数据去重并分成 20 条批次，账号改变后停止后续批次。87 项本地测试通过，包含事务中途失败回滚、并发幂等、发送消息不能冒充收件消息及分批会话隔离。真实 CloudBase 事务验证尚未执行；已读回执同步、自动失败重试及发送流程迁移仍需完成。

R3 增量同步：同步查询和记录投影已迁入严格 TS 用例，与历史读取共享私有字段剥离和会话/参与者验证；客户端验证游标范围、递增顺序及响应边界。消息与评论轮询修复了“第二页失败但第一页游标已提交”的漏更新问题：整批成功交付页面后才保存游标。84 项本地测试通过，包括分页故障重试、错误游标拒绝、匿名投影和数据库前置拒绝。发送、已读事务和通知仍待迁移；已读回执同步以及超过 100 条收件消息的分批标记是下一批需要解决的明确缺口。

R3 历史读取：getConversation 已迁入严格 TS 用例，删除 before/after 时间分页，改为会话作用域序号游标；首次同步从当前最新序号开始，不再重放全部历史。页面提供更早消息、稳定滚动锚点和分页重试，隐藏/销毁/换账号会丢弃迟到读取及发送结果。历史响应采用共享显式 DTO，删除私有元数据。目录校验同时对齐实际写入的普通会话 anonymous_context: null。79 项本地测试通过，覆盖 75 条同时间消息、新消息插入、错误范围、匿名脱敏、CloudBase 查询适配器与页面行为。线上旧消息仍需回填 conversation_id/sync_sequence；发送、同步、已读和通知迁移继续进行。

R3 服务端迁移：会话目录分页、账号作用域、数据库记录校验及公开摘要映射现由 packages/server 的严格 TypeScript 用例执行，CloudBase 入口通过窄接口提供有界列表和资料读取。匿名上下文丢失或参与者不匹配会失败，不再回退成普通会话。构建已嵌入 server 及 contracts 的完整运行时依赖，仓库外产物测试执行真实目录用例。74 项本地测试通过；发送、聊天历史、已读与通知仍在 JS 中，后续继续迁移，不视为整个消息模块完成。

R1/R2 新进展：会话目录的请求、游标、摘要与分页响应已接入共享 TypeScript 运行时校验，客户端不再仅依赖泛型断言。摘要采用显式字段映射。构建生成微信可编译契约，并把服务端契约嵌入独立云函数；两处上传根目录统一为 dist/cloudfunctions。72 项本地测试通过，新增畸形响应、非法 limit、匿名字段剥离、生成源码一致性及仓库外产物加载验证。其余动作 schema 和服务端 TS 迁移仍未完成，下一步继续迁移消息模块业务。

A06 角标修复：新增 verified 权限的 `messages.getUnreadMessageCount`，按可信 OPENID 对消息事实表执行未读 count；角标不再汇总前 50 个会话。schema 增加 `(to, status)` 索引，尚未云端应用。`npm run check` 通过 66 项测试，覆盖 240 条未读、调用者不能指定其他收件人及角标只请求计数。会话目录现已增加账号作用域的 `(updated_at, _id)` 排他游标，页面支持追加、去重和失败重试；隐藏、销毁与会话变更会丢弃迟到结果。完整检查通过 68 项测试，包含 53 条同时间会话遍历和跨账号游标拒绝。实时目录不是快照：翻页期间收到新消息的条目可能移到游标之前，重新进入页面刷新首页即可取得；真实云端索引与迁移仍未验证，R3 未完成。

最近验证：`npm run check` 通过 63 项测试。设置页取消重复会话写入，隐藏/销毁后不再渲染迟到资料结果；邮箱认证页在隐藏/销毁时取消倒计时和跳转，恢复时按截止时间计算冷却，并丢弃已失效请求的界面副作用。新增测试覆盖设置页三种生命周期及认证发送/验证返回后的页面销毁；服务端邮箱暂停状态未改变。该证据属于 A11 本地验证，不替代真机验收。

最新授权：以完整 v1.0 为目标自主实施，不保留旧协议向后兼容；旧计划中的兼容窗口要求已被用户覆盖。用户最新授权：完成并通过本地检查的批次自动提交 Git；GitHub Actions 未开启，不作为当前执行阻塞。云端历史数据的迁移与核对仍需执行，不能用删除兼容代码代替数据准备。

本轮 A11 修复：session 清理按步骤隔离存储和订阅者故障，显式登出后禁止同进程再次读取残留身份；资料更新、验证码认证和自动保存登录会话使用 revision 拒绝迟到结果。`npm run check` 实测通过 60 项测试，包含删除存储失败、监听器抛错及三类请求在登出/换账号后的竞态。跨进程存储故障恢复与微信设备验收尚未证明。

完成 R2 的动作策略与契约入口，并继续完成 R3 的消息迁移、目录回填与真实平台验证。后续长任务可直接使用 [goal-prompt.md](goal-prompt.md)，不必重新规划整个项目。

## 待核实事项

| 事项 | 谁/何时解决 | 当前处理 |
|---|---|---|
| 云端运行时、SDK、事务/索引真实能力 | R1 选择与 R8 实测 | 已完成 CLI 登录及只读线上基线；旧云函数运行在 Nodejs16.13，schema/权限及真实事务验收尚未完成 |
| 测试环境与设备工具可用性 | 执行者 R0/R1 发现 | 不阻止独立本地工作；只在已明确环境执行云端操作 |
| 目标包体/延迟/成本预算 | R0 测量，R7 固化 | 先保证有界读取、无重复轮询等结构性要求 |
| 邮件恢复、审核与保留政策 | 上线准备阶段 | 保持认证暂停，不将本地重构称为可直接上线 |

## 实施期间维护格式

每次更新阶段证据时写：变更入口/文件、关联 Axx 验收、真实命令与结果、运行环境、未通过原因。长日志不直接粘贴；可链接现有测试、报告或 CI 记录。

另在本页维护两份短清单：

- **消息旧协议已删除：** `messages.listConversations` 仅查询 `conversation_entries`，空目录返回空列表，缺集合明确失败；不再扫描历史 200 条消息拼目录。`syncConversation` 强制使用 v2 会话序号游标，删除 `since/nextSince` 同步协议；历史记录仍须离线回填后才可切换云端。`npm run check` 通过 64 项测试，覆盖缺失/非法游标、空目录及缺集合不回退。会话公开资料查询同时移除角色与认证字段。
- **发现的例外/阻塞：** 当前无阻止文档规划的阻塞；云端/设备状态未验证。新增时说明所需外部条件及可继续的独立工作。

已完成事项不要在下一次更新丢失，但用精简证据替代反复追加同义日记。本页是本轮唯一状态来源。

成员管理切片随后已部署admin云函数与/admin静态网页，CLI分别返回成功（dist/admin-members-function-deploy.log、admin-members-hosting-deploy.log）。未对真实管理员执行撤权或修改角色。
