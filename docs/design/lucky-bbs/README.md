# Superseded Design

The documents in this directory describe the original FastAPI/PostgreSQL/JWT/WebSocket implementation and are retained for historical traceability only. They are not deployment instructions.

The implemented architecture is the CloudBase design documented in [docs/DEVELOPMENT.md](../../DEVELOPMENT.md): authenticated WeChat cloud functions, private content collections, sanitized DTO reads, and `updated_at` polling for chat.
