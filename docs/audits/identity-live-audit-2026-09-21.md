# 云端身份数据与邮箱恢复准备

## 后续实施结果

2026-09-21 已完成真实身份迁移：10 条主账号、4 条邮箱归属、38 条重复记录归档后删除，原账号 ID 与认证保持；逐字段比对通过，私人 journal 标记 completed=true。旧 `_openid_1` 非唯一索引与新唯一索引冲突，已删除旧索引并创建 `unique_openid`。六个当前云函数已部署、18 个集合与客户端权限已应用；邮箱密钥和开关尚未配置。迁移工具为 scripts/apply-identity-migration.js；原始备份、归档与执行 journal 仅位于下述本地忽略目录。新项目交接和未完成项以 [状态文件](../plans/refactor/status.md) 顶部为准。

## 迁移前只读审计

2026-09-21 对配置环境 cloud1-d4g94y77f0a618eb5 执行只读查询和 users 导出。环境 NORMAL，CLI 账号有效。此次没有更新账号、删除记录、建立集合或部署函数。

users 导出包含 48 条记录，和独立 count 查询一致，对应 10 个不同微信身份。存在两组重复身份，分别有 37 条和 3 条记录；每组只有一条已认证记录。共 4 个已认证账号，已认证邮箱无重复，用户要求保留的 hvydw1 对应记录存在。43 条记录缺失 email/verified，所有记录缺失 profile_version；昵称、头像、角色等基本字段存在，角色均为 user。

原始导出仅保存在被 Git 忽略的本地目录 dist/private-backups/identity-20260921，未加入仓库。导出 SHA-256 为 `7ab2795e9eb7958d560105307201a8b79b3cc9cfe03c95f942d5220c2eba614b`。CLI 下载遇到本机网络错误后，使用同一导出文件完成下载，未重复生成数据快照；该备份不是云端持续备份或异地恢复演练的替代品。

实际运行 scripts/plan-identity-migration.js 生成本地私人迁移方案：保留 10 条账号主记录，4 条邮箱归属，38 条重复记录待归档，10 个主账号需要补默认字段。方案优先保留同一微信身份的唯一已认证记录及原文档 ID，保留所有原始记录；遇到多个已认证记录、角色冲突、邮箱冲突或无法确定的未认证重复记录则拒绝猜测。当前样本没有这些未解决冲突。

后续核对：旧 login 按 OPENID 文档 ID 读取，却用随机 ID 新增，且把读取异常当作不存在；旧 users 也使用随机 ID 新增。两个函数代码已下载到上述忽略目录备份。第二次只读查询与导出比较，在统一 BSON 日期表示后无新增、修改或缺失；这不是阻止后续写入的锁。

用户明确授权破坏性原型重构后，已执行 tcb fn delete login --json，返回 Function deleted，废弃 login 已删除。旧 users 仍存在，账号迁移尚未应用。执行迁移时须停止该旧写入入口并核对账号引用，保留指定账号、既有认证与备份，再清理重复记录。邮箱入口开启依赖归属迁移、集合权限、服务器密钥和核心云端流程验证。
